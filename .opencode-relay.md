# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-04 ~07:06 UTC run (opencode-schedule, no relay input).
- Previous run: 2026-08-04 ~03:51 UTC (status: complete, noctalia build pending).

## Completed this run
1. **Failed-build triage**: Two historical failures (30759520980, 30759890419) from Aug 2 — both already resolved. The quickshell-git `/usr/usr` path error and a transient `lame` download failure were fixed in subsequent builds. No action needed.
2. **Version inventory sweep (all 19 packages) — web/API verified current.**
   - Git snapshots: niri feb3e43 (up to date), noctalia e41c994 (up to date), quickshell 28771c7 (up to date), xwayland-satellite 8d135d3 (up to date).
   - Stable latest all confirmed: blender 5.2.0, brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5, helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4, msnap 0.6.1, noctalia-greeter 1.1.0, protonplus 0.5.22, sdbus-cpp 2.3.1, vesktop 1.6.5, zen 1.21.10b, rootapp 0.9.125, gcc16 16.1.1+20260801.
3. **Release consistency**: All 27 rolling release assets (19 packages + 8 gcc16 subpackages) match templates.
4. **Install-test sweep** (full coverage in ghcr.io/void-linux/void-glibc-full):
   - blender-bin: install OK, blender --version OK
   - brave-browser: install OK
   - brave-origin-bin: install OK, brave-origin --version OK
   - faugus-launcher: install OK, binary found
   - helium-browser-bin: install OK, helium --version OK
   - heroic-games-launcher: install OK, binary found
   - libspng: install OK, pkgver verified
   - msnap: install OK, binary found
   - noctalia: install OK, binary found
   - noctalia-greeter: install OK, binary found
   - protonplus: install OK, binary found
   - quickshell-git: install OK, quickshell --version OK
   - rootapp: install OK, binary found
   - sdbus-cpp: install OK, pkgver verified
   - vesktop: install OK, binary found
   - xwayland-satellite-git: install OK, binary found
   - zen-browser: install OK, zen --version OK
   - gcc16 + all subpackages: install OK, gcc16 --version OK
   - Fresh-checker re-run: 10 packages (heroic, helium, protonplus, blender, zen, gcc16, noctalia, quickshell, vesktop, rootapp) installed + smoke-tested on clean container — all pass.
5. **Issues/PRs**: no open issues/PRs.
6. **README inventory**: 19 packages listed, 19 real template directories — consistent.
7. **Churn check**: No duplicate version bumps detected. All auto-update commits carry unique versions.

## Pending
- Nothing. All work complete.

## Final state
- Working tree clean.
- No builds triggered (nothing needed updating).
- No churn.
