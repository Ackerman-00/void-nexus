# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-04 ~18:12 UTC run (opencode-schedule, no relay input). Fresh independent
  verification of prior "complete" state; did not trust prior claims.
- Branch: worked on main. Commit pushed: 3b2d249.

## Failed-build triage
- Recent "Build Nexus XBPS": SUCCESS (18m runs).
- Recent "Check and Update Package Versions": SUCCESS every cycle; last at 17:38 UTC
  -> "All binaries already exist. No build needed." (no churn).
- Recent opencode-schedule run (15:04, 30922255574) failed ONLY in the non-critical
  "Clean up stray opencode/schedule branches" cleanup step (git fetch auth after long
  run). This is infra, no package work lost; relay was updated+committed before that.
  No open failure issue (auto-error-opener didn't fire).

## Version inventory sweep (all 19 packages + 8 gcc16 subpackages) - RE-VERIFIED this run
Independent re-verification, all sources checked this run (GH API via gh api, git
ls-remote, upstream release pages, distfile download+sha256sum, AppImage/deb/tar
internal version reads):
- Git snapshots @ upstream HEAD (git ls-remote): niri feb3e43 = HEAD ✓,
  noctalia e41c994 = HEAD ✓, quickshell 28771c7 = HEAD ✓, xwayland-satellite
  8d135d3 = HEAD ✓.
- Stable (upstream latest + distfile sha256 EXACT template match):
  blender 5.2.0 ✓(96f6c...), brave-browser 1.93.129 ✓(7ceca...),
  brave-origin-bin 1.93.129 ✓(ad79b...), faugus 2.0.5 ✓(0599d...),
  helium 0.15.1.1 ✓(d51ae...), heroic 2.22.0 ✓(5aa12...), libspng 0.7.4 ✓(47ec0...),
  msnap 0.6.1 ✓(bfc2c...), noctalia-greeter 1.1.0 ✓(771d2...),
  protonplus 0.5.22 ✓(85c3a...), rootapp 0.9.125 ✓(AppImage sha256 86ed0d... EXACT;
    X-AppImage-Version + sq.version both 0.9.125; fedora-nexus spec 0.9.125),
  sdbus-cpp 2.3.1 (metapkg, Void ships sdbus-c++ 2.3.1_1) ✓, vesktop 1.6.5 ✓(2d4c9...);
  zen-browser 1.21.10b ✓(tar 6a134be... + LICENSE c76f74..., both verified),
  gcc16 16.1.1+20260801 ✓(latest snapshot; 16-20260802 404, so 16-20260801 is current).
- brave NOTE: /releases/latest = v1.93.129 (non-prerelease). v1.95.x tags are Nightly
  channel (no stable binaries); not a version miss.
- Multi-arch: vesktop aarch64 deb sha256 3ba8e5... = template ✓.

## Change made this run (ONLY change)
- srcpkgs/zen-browser/template: removed stale, UNUSED `_checksum` variables
  (87978f.../78d45f...). They were dead code (never referenced by template,
  do_install, or check-updates.yml's get_arch_checksum which is also unused), and the
  hashes were WRONG (did not match the authoritative, verified checksum= line
  6a134be.../c76f74...). Pure cleanup; version/revision UNCHANGED (1.21.10b_1) so no
  rebuild required and rolling binary zen-browser-1.21.10b_1 already present.
  Commit 3b2d249 pushed to main.

## Release consistency
- All 27 rolling assets (19 pkgs + gcc16 + 8 subpkgs) exactly match expected
  `<pkg>-<ver>_<rev>.x86_64.xbps`. No missing/stale binaries.

## Install-test sweep (FRESH, this run - clean ghcr.io/void-linux/void-glibc-full)
Built local repo from rolling binaries + trusted public.pem; for EACH package a fresh
container, synced main Void repo + no local nexus repo, `xbps-install -y <pkg>`.
ALL 19 packages + 8 gcc16 subpackages: xbps-install clean (RC 0), smoke OK:
- Smoke: blender-bin (`blender --version`=5.2.0), brave-origin-bin (`brave-origin
  --version`=Brave Origin 151.1.93.129), brave-browser (`brave-browser-stable
  --version`=Brave Browser 151.1.93.129 AFTER installing bash; wrapper is #!/bin/bash,
  bash is Void base), helium (Helium 0.15.1.1), heroic, vesktop, zen (Mozilla Zen
  1.21.10b), rootapp (Avalonia starts), protonplus (GTK4 app, no launchers error
  expected headless), faugus-launcher, msnap, niri (`niri 26.04`), noctalia
  (v5.0.0), noctalia-greeter, quickshell-git, gcc16 (16.1.1 20260801), xwayland-
  satellite-git (daemon, library entry present).
- Lib-only/skip-smoke: libspng, sdbus-cpp (pulls real sdbus-c++ 2.3.1_1+basu), and
  the 8 gcc16 subpackages (libs).

## Dependency audit (every package reconciled this run)
All built .xbps props.plist parsed (tar-extracted): each package's run_depends =
explicit template depends + auto-resolved shlib requires (via void-packages common/
shlibs). Asserted consistent with templates for ALL packages. Key notes:
- brave-browser has NO template `depends` line but is CORRECT: build auto-resolves
  qt5/qt6/X/alsa/gtk/cups/gbm/nss/nspr/at-spi/pango/eudev from shlibs
  (verified: props.plist run_depends has all 31). No fix needed.
- sdbus-cpp metapackage depends `sdbus-c++>=2.3.1_1` resolves to real Void pkg
  (installed OK + basu).
- No missing/extra deps found in any template. status: deps-verified (all).
- NOT re-teared upstream source trees this run beyond checksum+version (upstream
  versions unchanged since last audit); shlib/run_depends derived from the ACTUAL
  published .xbps, which is authoritative for what ships.

## Issues / PRs
- No open issues. No open pull requests.

## README
- 19 README packages == 19 real srcpkgs dirs (8 gcc16 subpkgs are symlinks).
  Match verified via comm. Nothing stale.

## Churn / no-churn
- No version/revision bumps. One cleanup commit (zen-browser dead vars). No rebuild
  triggered (binary already in rolling, version unchanged). No duplicate auto-updates.

## Pending
- Nothing.

## Final state
- One cleanup commit pushed (3b2d249). Working tree clean.