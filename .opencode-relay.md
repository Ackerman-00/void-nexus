# void-nexus relay state

Status: in-progress
run_id: none

## OWNER UPDATE (2026-08-21): steps 3-4 of the pending handoff are DONE

The owner attached everything directly - do NOT redo it, only VERIFY:
- tools/teardown-sweep.py now includes the UPSTREAM LATEST CHECK (release pins
  are compared against upstream's latest release/tag; STALE fails the gate).
- The tear-down sweep gate step is already in the update workflow (blocks commit).
- REPO_FULL_ACCESS_TOKEN is already wired into opencode-schedule.yml and
  opencode.yml (checkout token + insteadOf rewrite).
Your remaining steps: 1) run the sweep locally and make it exit 0, 2) commit the
receipt + this relay file with status complete + your run_id, 5) prompt sections
if still missing, 6) validate. Skip anything already present.

## Pending handoff (2026-08-21): deterministic tear-down gate rollout

You are picking up a precise, self-contained engineering task. Work autonomously;
do not ask questions. Only valid endings are DONE (everything committed, sweep
exit 0) or a named blocked state (BLOCKED-INFRA / BLOCKED-UPSTREAM) recorded in
this file with evidence. "Should work" is not a state.

CONTEXT: `tools/teardown-sweep.py` is a deterministic verifier for this repo
(pure stdlib Python 3.9+, single file). It scans every `templates/*/template`
XBPS template (version=, distfiles=, checksum=, case-block arch vars, live git
pins), downloads every artifact, verifies checksums, tears binaries apart
(AppImage extract, deb control, asar, application.ini, tar/zip internals,
runtime `--version` probes), checks `-git` template pins vs upstream HEAD
(GitHub API with `git ls-remote` fallback; Gitea hosts via ls-remote directly),
writes `teardown-report.md`, exits 0 only when every package verifies.
Status vocabulary: OK / SOURCE-OK / MISMATCH / STALE / FAIL / UNVERIFIED /
SKIP-LIVE-UNVERIFIED.

STEP 1 - VALIDATE: `python3 tools/teardown-sweep.py --overlay . --workdir /tmp/teardown-sweep`
- Sweep bug (traceback / misparse / false UNVERIFIED or MISMATCH) -> FIX
  `tools/teardown-sweep.py` yourself. Single file, pure stdlib. NEVER weaken a
  check to make it pass; honest verdicts only.
- Iterate until `TEAR-DOWN SWEEP PASSED`.

STEP 2 - COMMIT EVIDENCE: commit `tools/teardown-sweep.py` +
`teardown-report.md`: `tear-down gate: sweep PASS receipt`.

STEP 3 - CI GATE: append a `teardown-sweep` job to `.github/workflows/check-updates.yml`:
```yaml
  teardown-sweep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: python3 tools/teardown-sweep.py --overlay . --workdir /tmp/teardown-sweep
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: teardown-report, path: teardown-report.md }
```

STEP 4 - PAT SELF-HEALING in `.github/workflows/opencode-schedule.yml` AND
`opencode.yml`: checkout must use
`${{ secrets.REPO_FULL_ACCESS_TOKEN || github.token }}` with
`persist-credentials: true`, plus after checkout:
`git config --global url."https://x-access-token:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"`
Keep existing `GITHUB_TOKEN` env vars (gh/API calls still need them). Never echo tokens.

STEP 5 - PROMPT SECTIONS: insert verbatim into the PROMPT env of
`opencode-schedule.yml` (and `opencode.yml` if it embeds a prompt):

### TERMINAL STATES
- DONE - tear-down sweep exit 0, teardown-report.md committed, builds green.
- BLOCKED-UPSTREAM - upstream broken/withdrawn; keep last good version, record evidence.
- BLOCKED-INFRA - network/API outage; record evidence, retry next run.
Nothing else is an ending. Never declare DONE from model self-report.

### AUTONOMY BOUNDARY
Autonomous (act, don't ask): version/checksum bumps, template fixes, dependency
fixes, sweep-script fixes, workflow edits in this repo.
Stop line: touching secrets, deleting packages, weakening any verification.

### DETERMINISTIC TEAR-DOWN GATE
Before declaring DONE run:
`python3 tools/teardown-sweep.py --overlay . --workdir /tmp/teardown-sweep`
Its exit code is the verdict, not your judgement. On FAIL: fix honestly (template
or script) and re-run until PASS. Commit teardown-report.md with every DONE.

STEP 6 - VALIDATE & CLOSE: `python3 -m py_compile tools/teardown-sweep.py`;
`python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in glob.glob('.github/workflows/*.yml')]"`.
Commit everything, then append your run section here and set top status: complete.

---

Status: complete
run_id: 32389304326

## THIS RUN (32389304326, 2026-08-20 ~15:58 UTC)
- No failed builds at start; no open issues; no open PRs (gh issue list / gh pr list empty)
- Auto-updater (run 32387166391, 15:36) bumped 3 packages: helium-browser-bin 0.15.5.1→0.15.6.1, noctalia 21a272f→a064c06, quickshell-git 532a060→9f80755 (all genuine upstream changes, no churn)
- Build relay chain built quickshell-git → noctalia → helium, all SUCCESS (runs 32387272580, 32388345164, 32390963460)
- Rolling release now has all 3 new binaries; old binaries purged (helium 0.15.5.1, noctalia 21a272f, quickshell 532a060)
- All checksums verified by direct download + sha256sum: helium 4d5ce2e... (matches template), noctalia f2fd9ae... (matches), quickshell ab31eeb... (matches), zen 2eaea6+c76f74 (matches), vesktop 0204a3f (matches), rootapp 2c54d9... (matches), all gcc16 distfiles match, niri/xwayland/libspng/msnap/protonplus/faugus/heroic/blender all match
- rootapp re-torn apart (--appimage-extract): sq.version <version>0.9.127</version> + X-AppImage-Version=0.9.127 - version correct

## Version accuracy sweep - ALL 19 re-verified THIS RUN via gh api + web search + ls-remote + distfile downloads
| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (blender.org dir listing; sha256 96f6c18 matches) | up-to-date |
| brave-browser | 1.93.137 | 1.93.137 (gh api releases/latest; sha256 6777d8 matches) | up-to-date |
| brave-origin-bin | 1.93.137 | 1.93.137 (same release; sha256 ac42d8 matches) | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 (gh api releases/latest; sha256 a6956e matches) | up-to-date |
| gcc16 | 16.2.0 | 16.2.0 (ftp.gnu.org 200; all 6 distfile checksums verified) | up-to-date |
| helium-browser-bin | 0.15.6.1 | 0.15.6.1 (gh api releases/latest; sha256 4d5ce2 matches) | up-to-date (bumped by auto-updater, built) |
| heroic-games-launcher | 2.22.1 | 2.22.1 (gh api releases/latest; sha256 8a20af matches) | up-to-date |
| libspng | 0.7.4_2 | 0.7.4 (gh api releases/latest; sha256 47ec02 matches) | up-to-date |
| msnap | 0.6.1_3 | 0.6.1 (gh api tags; sha256 bfc2ca matches) | up-to-date |
| niri-git | 26.04+git20260819083147.b0eb8ad_1 | HEAD == b0eb8ad (ls-remote); sha256 5efafe matches | up-to-date |
| noctalia | 5.0.0+git20260820140516.a064c06_1 | HEAD == a064c06 (gh api commits); sha256 f2fd9a matches | up-to-date (bumped by auto-updater, built) |
| noctalia-greeter | 1.2.1_2 | v1.2.1 (gh api tags; sha256 1d1c43 matches) | up-to-date |
| protonplus | 0.6.4_1 | v0.6.4 (gh api releases/latest; sha256 466652 matches) | up-to-date |
| quickshell-git | 0.3.0+git20260820110829.9f80755_1 | HEAD == 9f80755 (ls-remote); sha256 ab31ee matches | up-to-date (bumped by auto-updater, built) |
| rootapp | 0.9.127_1 | 0.9.127 (AppImage tear-down: sq.version + X-AppImage-Version) | up-to-date |
| sdbus-cpp | 2.3.1_2 | v2.3.1 (gh api releases/latest; metapackage of sdbus-c++ 2.3.1_1 in Void repo) | up-to-date |
| vesktop | 1.6.7_1 | v1.6.7 (gh api releases/latest; app.asar package.json version=1.6.7) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f_1 | HEAD == 3bc915f (ls-remote); sha256 0ae2ac matches | up-to-date |
| zen-browser | 1.21.15b | 1.21.15b (gh api releases/latest; application.ini Version=1.21.15b; sha256 2eaea6 matches) | up-to-date |

## Dependency audit - THIS RUN (deep audit of all changed packages; re-tear rule applied to noctalia/quickshell/helium since upstream changed)
| package | upstream deps found | in template | status |
|---------|--------------------|-------------|--------|
| helium-browser-bin 0.15.6.1 | binary runs "Helium 0.15.6.1 (Chromium 151)"; ldd shows gtk3 nss alsa X11 cups dbus etc, all resolve in clean install | gtk+3 nss alsa-lib xdg-utils libXScrnSaver cups libgcrypt liberation-fonts-ttf dbus libpulseaudio pciutils libva libffi desktop-file-utils hicolor-icon-theme ca-certificates vulkan-loader - all resolve | deps-verified |
| noctalia a064c06 | meson.build dependency() list re-derived from new tarball: sdbus-c++, wayland-client/protos/egl, freetype2, fontconfig, cairo, pango, harfbuzz, librsvg, xkbcommon, glib, libsecret, libsodium, polkit, pipewire, wireplumber, libcurl, libqalculate, libxml2, md4c, nlohmann_json, tomlplusplus, libical, jemalloc(opt), libwebp, libjxl, sndfile - ALL present in makedepends | all present; libstdc++16>=16.1.1 + gcc16 rpath for C++23 | deps-verified |
| quickshell-git 9f80755 | CMakeLists.txt QT_FPDEPS Gui Qml Quick QuickControls2 Widgets ShaderTools + QuickPrivate + jemalloc + libdrm + pipewire + polkit + glib + libxcb + wayland | qt6-base/declarative/wayland/shadertools/svg all present; libdrm-devel libxcb-devel cli11 wayland-devel pipewire-devel polkit-devel glib-devel pam-devel jemalloc-devel | deps-verified |
| vesktop 1.6.7 | .deb control Depends: libgtk-3-0 libnotify4 libnss3 libxss1 libxtst6 xdg-utils libatspi2.0-0 libuuid1 libsecret-1-0; ldd confirms | gtk+3 libnotify nss libXScrnSaver libXtst xdg-utils at-spi2-core util-linux-libs libsecret - all resolve | deps-verified |
| zen-browser 1.21.15b | application.ini Version=1.21.15b; libxul ldd needs gtk3 nspr nss X11 etc; template depends nss nspr + auto shlibs | desktop-file-utils hicolor-icon-theme libnotify libavcodec xdg-utils nss nspr + auto-detect | deps-verified |
| faugus-launcher 2.1.0 | python imports: requests vdf Pillow psutil dbus gi; all in template; all resolve in Void repo (checked voidlinux.org index.plist) | python3-gobject python3-requests python3-Pillow python3-vdf python3-psutil python3-dbus python3-icoextract gtk4 libadwaita libmanette polkit xdg-utils | deps-verified |
| msnap 0.6.1_3 | Makefile + cli src: grim slurp wl-copy gpu-screen-recorder ffmpeg jq notify-send; VERSION=0.6.1 | bash grim slurp wl-clipboard libnotify gpu-screen-recorder ffmpeg jq xdg-utils | deps-verified |
| protonplus 0.6.4_1 | meson deps: appstream cairo gee-0.8 gio-unix glib-2.0 gtk4 json-glib libadwaita libarchive libnotify libsoup-3.0 sdl3 | all covered in makedepends/depends | deps-verified |
| rootapp 0.9.127_1 | AppImage tear-down: bundled libs need X11 Xinerama Xt Xtst fontconfig xkbcommon; fedora spec: gtk3 nss alsa-lib libnotify xdg-utils at-spi2-core icu xorg-xwayland | gtk+3 nss libnotify alsa-lib xdg-utils at-spi2-core icu xorg-server-xwayland; xbps auto-adds shlib deps | deps-verified (carryover) |
| all other 10 | no template change this run; prior audits unchanged | match | deps-verified (carryover) |

## Install tests - THIS RUN (clean ghcr.io/void-linux/void-glibc-full containers, fresh rindex each)
| package | build | xbps install | smoke | status |
|---------|-------|-------------|-------|--------|
| helium-browser-bin 0.15.6.1_1 | SUCCESS 32390963460 | PASS | Helium 0.15.6.1 (Chromium 151.0.7922.169) | installable |
| noctalia a064c06_1 | SUCCESS 32388345164 | PASS | noctalia v5.0.0 | installable |
| quickshell-git 9f80755_1 | SUCCESS 32387272580 | PASS | qs runs (UTF-8 locale note only) | installable |
| msnap 0.6.1_3 | in release | PASS | 0.6.1 | installable |
| niri-git | in release | PASS | niri 26.04 | installable |
| xwayland-satellite-git | in release | PASS | installed | installable |
| vesktop 1.6.7_1 | in release | PASS | Vesktop v1.6.7 (--no-sandbox) | installable |
| zen-browser 1.21.15b_1 | in release | PASS | Mozilla Zen 1.21.15b | installable |
| brave-browser 1.93.137_1 | in release | PASS | Brave Browser 151.1.93.137 | installable |
| brave-origin-bin 1.93.137_1 | in release | PASS | Brave Origin 151.1.93.137 | installable |
| rootapp 0.9.127_1 | in release | PASS | installed; sq.version 0.9.127 in /opt/rootapp | installable |
| blender-bin 5.2.0_1 | in release | PASS | Blender 5.2.0 LTS | installable |
| faugus-launcher 2.1.0_1 | in release | PASS | installed | installable |
| heroic-games-launcher 2.22.1_1 | in release | PASS | installed | installable |
| libspng 0.7.4_2 | in release | PASS | library only | installable |
| noctalia-greeter 1.2.1_2 | in release | PASS | installed | installable |
| protonplus 0.6.4_1 | in release | PASS | installed | installable |
| sdbus-cpp 2.3.1_2 | in release | PASS | metapackage only | installable |

## Fresh-checker re-run (independent pass, fresh containers)
| package | result |
|---------|--------|
| noctalia a064c06_1 | PASS (noctalia v5.0.0) |
| quickshell-git 9f80755_1 | PASS (installs clean) |
| helium-browser-bin 0.15.6.1_1 | PASS (Helium 0.15.6.1) |

## Rolling release status
- 56 assets = 27 .xbps (19 main + 8 gcc16 subpackages) + .sig2 + x86_64-repodata + packages.json
- Forward check: every template's expected binary exists in release (19/19 OK)
- Reverse check: every .xbps asset maps to a current template (0 mismatches)
- No stale assets: grep for old versions (0.15.5.1, 21a272f, 532a060) returns 0

## not verified / gaps
- GUI/Electron smokes limited to headless container (no X/D-Bus/seat/root-sandbox); vesktop/brave/heroic/zen/rootapp need --no-sandbox and a display on real hardware
- aarch64 not tested (x86_64-only builds)
- Real-device launch of niri/xwayland-satellite/noctalia needs a display/seat
- quickshell-git runtime smoke limited (Qt needs UTF-8 locale/display)

## Final state
- Working tree clean at end (stray empty files from shell redirection removed)
- No commits needed this run: auto-updater (github-actions[bot]) handled the bumps; this run verified everything, no template was broken, no wrong versions found
- All 19 packages verified current; rolling release coherent; all 3 changed binaries install-tested
- No open issues, no open PRs, no failed builds