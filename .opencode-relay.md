# void-nexus relay state

Status: complete
run_id: 31617462148

## Run context
- 2026-08-12 ~16:25 UTC run on main (RELAY=false, fresh). Auto-updater had just
  committed 8080a2f (bump noctalia, helium-browser-bin, niri-git) and triggered a
  relay build queue; this run verified those changes, swept the whole inventory,
  and install-tested the changed binaries.

## Auto-update verification (commit 8080a2f) - ALL VERIFIED UPSTREAM
- noctalia: `_commit` 8403cb98 == upstream HEAD (git ls-remote). version string
  `5.0.0+git20260812150035.8403cb9`; `_gitdate` == upstream commit date
  2026-08-12T15:00:35Z. Distfile sha256 f27c6ca matches template.
- helium-browser-bin: 0.15.4.1 == upstream latest release tag (GH API).
  Binary self-reports `Helium 0.15.4.1 (Chromium 151.0.7922.137)`. Distfile
  sha256 ab1f761 matches template.
- niri-git: `_commit` 92a25a0 == upstream HEAD (git ls-remote). `_gitdate`
  == upstream commit date 2026-08-12T12:53:30Z. Distfile sha256 174f7e03
  matches template.

## Relay build queue (auto-updater triggered) - ALL SUCCEEDED
- 31615516364 (noctalia) success -> relayed
- 31617837562 (helium-browser-bin + niri-git) success -> relayed
- 31618247117 (niri-git) success
- All three binaries published to "rolling"; old versions purged:
  noctalia-5.0.0+git20260812150035.8403cb9_1,
  helium-browser-bin-0.15.4.1_1, niri-git-26.04+git20260812125330.92a25a0_1.
- No old-version assets remain (checked grep for previous versions).

## Version accuracy sweep (all 19 packages) - ALL up-to-date
Queried upstream GitHub API releases/latest + `git ls-remote HEAD` + blender.org
+ rootapp AppImage internals. Every template value matches upstream HEAD/tag:

| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (Blender5.2 dir) | up-to-date |
| brave-browser | 1.93.134 | v1.93.134 | up-to-date |
| brave-origin-bin | 1.93.134 | v1.93.134 | up-to-date |
| faugus-launcher | 2.0.6 | 2.0.6 | up-to-date |
| gcc16 | 16.1.1+20260801 | snapshot 16-20260808 live | DELIBERATE GAP (excluded by automation; multi-hour manual build, see below) |
| helium-browser-bin | 0.15.4.1 | 0.15.4.1 | up-to-date |
| heroic-games-launcher | 2.22.1 | v2.22.1 | up-to-date |
| libspng | 0.7.4 | v0.7.4 | up-to-date |
| msnap | 0.6.1 | v0.6.1 | up-to-date |
| niri-git | 26.04+git92a25a0 | HEAD == 92a25a0 | up-to-date |
| noctalia | 5.0.0+git8403cb9 | HEAD == 8403cb9 | up-to-date |
| noctalia-greeter | 1.2.1 | tag v1.2.1 (no \"prerelease latest\"; latest tag) | up-to-date |
| protonplus | 0.6.3 | v0.6.3 | up-to-date |
| quickshell-git | 0.3.0+git28771c7 | HEAD == 28771c7 | up-to-date |
| rootapp | 0.9.126 | 0.9.126 (fedora-nexus spec + AppImage sq.version) | up-to-date |
| sdbus-cpp | 2.3.1 (metapackage) | v2.3.1 | up-to-date |
| vesktop | 1.6.5 | v1.6.5 | up-to-date |
| xwayland-satellite-git | 0.8.2+git3bc915f | HEAD == 3bc915f | up-to-date |
| zen-browser | 1.21.13b | 1.21.13b | up-to-date |

## Distfile checksum verification - THIS RUN (all 19 + gcc16 deps)
All distfiles downloaded and sha256sum'd; every checksum matches its template:
blender (96f6c1 via URL 200 + prior build), brave deb (ba1770), brave-origin
(12e021), faugus (5b90ec), helium (ab1f761), heroic (8a20af), libspng (47ec02),
msnap (bfc2ca), niri (174f7e03), noctalia (f27c6ca), noctalia-greeter (1d1c43),
protonplus (08b75f), quickshell (dd938be), rootapp (073692, AppImage torn
apart: sq.version=0.9.126, X-AppImage-Version=0.9.126), vesktop (2d4c97),
xwayland-satellite (0ae2ac), zen tar (d54b0b) + zen LICENSE (c76f74), gcc16 all
6: 2519f6, a3c2b8, b67ba0, ab6424, 626335, 108897 (isl needs the libisl
SourceForge path; voidlinux mirror returns 404, which is why XBPS fallback is
configured).

## Install-self-test - THIS RUN (all replaced+changed packages)
Clean ghcr.io/void-linux/void-glibc-full:latest containers (fresh image pulled
this run) with pristine signed repodata downloaded from the rolling release.
- Full 19-package transaction: 336 downloaded, 361 installed, 361 configured,
  RC 0.
- 8 gcc16 subpkgs installed: 8 configured, RC 0.
- sdbus-cpp metapackage resolves sdbus-c++-2.3.1_1 present in Void repo.
- Smoke: blender 5.2.0 LTS; brave-browser 151.1.93.134 (script needs bash,
  standard on Void); brave-origin 151.1.93.134; helium 0.15.4.1;
  noctalia v5.0.0; niri 26.04; msnap help; gcc16 16.1.1 20260801;
  quickshell 0.3.0; zen 1.21.13b (needs --no-sandbox headless, expected);
  vesktop v1.6.5 (dbus warn headless, expected); heroic/rootapp/windowed apps
  exit non-zero headless (no D-Bus/session) - expected, recorded as gap.
- Fresh-checker pass: new container re-installed noctalia/helium/niri from
  pristine signed repo, all clean (198 configured, RC 0).

## Dependency deep audit - THIS RUN (changed packages re-derived)
AUTO-UPDATED PACKAGES RE-TORN (RE-TEAR rule): new binaries opened and
props.plist read (tar --zstd -xOf) for the three changed packages:

| package | binary run_depends (props.plist) | template depends | verdict |
|---------|----------------------------------|------------------|---------|
| helium-browser-bin 0.15.4.1 | gtk+3 nss alsa-lib xdg-utils libXScrnSaver cups libgcrypt liberation-fonts-ttf dbus libpulseaudio pciutils libva libffi desktop-file-utils hicolor-icon-theme ca-certificates vulkan-loader + auto glibc/qt5-*/qt6-*/libX11-*/etc | template lists the explicit set; all resolve | deps-verified (high) |
| noctalia 5.0.0+git8403cb9 | libstdc++16>=16.1.1 + libEGL/libGLES/glibc/cairo/libcurl/fontconfig/freetype/glib/harfbuzz/jemalloc/libjxl/tomlplusplus/pam/sdbus-c++/wayland/... | matches makedepends-derived shlib set; gcc16 LINK/rpath from template | deps-verified (high) |
| niri-git 92a25a0 | wayland, xwayland-satellite-git + auto glibc/cairo/libdisplay-info/libgbm/glib/libinput/pango/pipewire/pixman/libseat/udev/xkbcommon | template depends wayland + xwayland-satellite-git; rest auto | deps-verified (high) |

Template shlib auto-resolution covers the remainder; every run_depends in each
built .xbps resolved during install (no missing-shlib errors) in clean containers.
sdbus-cpp metapackage depends sdbus-c++>=2.3.1_1 confirmed present.

## Install-test ledger (update every test)
| package | last install-tested | result | smoke |
|---------|--------------------|--------|-------|
| ALL 19 packages + 8 gcc16 subpkgs | 2026-08-12 this run | PASS (361 configured RC 0; subpkgs 8 configured RC 0) | version strings verified for blender/brave/helium/noctalia/niri/msnap/gcc16/quickshell/zen/vesktop |
| fresh-checker re-run (noctalia/helium/niri) | 2026-08-12 this run | PASS (198 configured RC 0) | n/a |

## Issues / PRs
- No open issues. No open PRs. Nothing to triage.

## Failed builds
- 31557225606 failed at 02:36Z 2026-08-12 (noctalia, <flat_set> C++23, pre-gcc16
  fix commit a6f80cf). Root cause already fixed in previous run's commit a6f80cf;
  all subsequent noctalia builds succeeded (31561851606, 31615516364). No action.

## gcc16 gap (deliberate, no churn)
- Upstream snapshot 16-20260808 exists; template pinned 16-20260801. NOT bumped:
  check-updates.yml excludes gcc16 (multi-hour build, manual only). All 6 current
  distfiles verified by download (checksums match template). Keep on this policy.

## not verified / gaps
- Headless container HW: GUI electron/chromium apps (heroic, rootapp, vesktop,
  brave full-launch) cannot launch without D-Bus/seat; smoke limited to
  binary/version output. Not a package defect.
- gcc16: built binary from 16-20260801 verified installable; latest snapshot
  not rebuilt (deliberate, see above).
- noctalia-greeter/Zen aarch64 branches not install-tested (x86_64 runner only).
- browser arrests (adversarial): no template touched this run; all package
  values independently re-verified from upstream API/source, not from sibling
  repos.

## Final state
- Working tree clean before/after (git status --porcelain empty). No commits
  this run (verification + install-test only; auto-update commit 8080a2f was
  already correct).
- All 19+8 templates pass `bash -n`. README table (19) matches srcpkgs (19)
  exactly.
- Rolling release coherent: 56 assets = 27 xbps + 27 sig2 + x86_64-repodata +
  packages.json; matches every template's evaluated version/revision.