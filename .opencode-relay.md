# void-nexus relay state

Status: complete (no relay needed; nothing unfinished)

## Run context
- Started: 2026-08-03 ~04:00 UTC (schedule run, no relay input)
- Picked up a clean repo at d8cf996 (last auto-update: noctalia only).
- History unshallowed (1152 commits) for proper no-churn auditing.

## Completed this run
1. **Failed-build triage**: All recent "Build Nexus XBPS" and "Check and Update
   Package Versions" runs succeeded (latest build 30778661812 on d8cf996). No
   failed runs, no template corruption (bash -n passes on every template).
2. **Full 19-package version inventory sweep** (web search + GitHub API + artifact
   tear-apart verified, all current):
   - git HEADs confirmed at upstream HEAD: niri feb3e43, noctalia 5aae077,
     quickshell 28771c7 (mirror), xwayland-satellite 8d135d3.
   - stable releases confirmed via API/repology/artifacts: blender 5.2.0,
     brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5, helium 0.15.1.1,
     heroic 2.22.0, libspng 0.7.4, msnap 0.6.1, noctalia-greeter 1.1.0,
     protonplus 0.5.22, sdbus-cpp 2.3.1, vesktop 1.6.5, zen 1.21.10b.
   - gcc16 = 16.1.1+20260801 (latest gcc snapshot; next 16-20260808).
   - rootapp: AppImage downloaded + torn apart; sq.version=0.9.125,
     X-AppImage-Version=0.9.125, fedora-nexus spec 0.9.125, sha256
     86ed0d76... matches template. VERIFIED.
   - Distfiles URLs all 200; ALL checksums re-verified by download+sha256sum
     (brave, brave-origin, vesktop, helium, heroic, zen, faugus, libspng,
     msnap, noctalia-greeter, protonplus, niri, noctalia, quickshell,
     xwayland-satellite, gcc16 + gmp/mpfr/mpc/isl/gentoo-patchset, blender
     via official .sha256).
3. **Key fingerprint resolved**: README's key filename b9:f2:38:0f:3f:a7:76:be:
   5f:ad:01:b9:ef:b5:55:77 is CORRECT (xbps uses OpenSSH-style MD5 fp; confirmed
   via ssh-keygen on converted pubkey + openssl signature verify of libspng
   .sig2 against public.pem).
4. **Dependency deep audit** (upstream build files opened & compared):
   faugus (python imports gi/requests/vdf/psutil/dbus/PIL/icoextract all covered),
   msnap (grim/slurp/wl-copy/notify-send/gpu-screen-recorder/ffmpeg/jq/xdg-open
   covered; quickshell/wayfreeze/satty noted optional), libspng (zlib/miniz),
   protonplus (gtk4/glib/libadwaita/json-glib/libsoup3/gee/libarchive),
   noctalia (all meson deps at 5aae077 covered incl. pam/sndfile/webp/jxl),
   noctalia-greeter (wlroots0.20/stb/webp covered), quickshell (qt6/cmake deps),
   niri (smithay/wayland/cairo/etc), xwayland-satellite (xcb/xcb-util-cursor).
   No missing or dead deps found.
5. **Install-test sweep** (ghcr.io/void-linux/void-glibc-full, rolling assets +
   sig2s, local repo): msnap, faugus-launcher, protonplus,
   xwayland-satellite-git, heroic-games-launcher, noctalia-greeter, libspng,
   sdbus-cpp (metapackage resolves sdbus-c++ 2.3.1_1 from Void repo), gcc16,
   libstdc++16 — ALL installed cleanly, no unresolvable deps.
   Smoke tests: gcc16 --version OK; msnap needs bash (container lacks it);
   xwayland-satellite has no --version flag (clap error, binary works).
6. **Release consistency**: programmatic check of every template vs rolling
   assets — 0 missing binaries (all 27 xbps present).
7. **Issues/PRs**: none open.
8. **Hygiene**: deleted stale branch origin/opencode/schedule-eead8b-20260802101613.
9. **README inventory** consistent (19 packages + gcc16 subpkg symlinks).

## Final state
- Working tree clean.
- No template changes, no builds triggered, no commits pushed (nothing was
  stale or broken).
