# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-03 ~18:18 UTC run (opencode-schedule, no relay input).
- Shallow clone unshallowed (1157 commits) for no-churn / git-history audit.

## Completed this run
1. **Failed-build triage**: Recent "Build Nexus XBPS" runs (30778661812, 30822619974,
   30827990924) and check-updates runs (30838204991, 30822553577, 30808848380, 30791722253)
   all succeeded. Only red run (30795597764) was an opencode-schedule that died on a
   transient 503 model-queue error (infra). No template corruption to fix.
2. **Version inventory sweep (all 19 real packages) — web/API/artifact verified current.**
   - Git snapshots all match upstream default-branch HEAD (checked via git ls-remote):
     niri feb3e43, quickshell-mirror 28771c7, xwayland-satellite 8d135d3,
     noctalia 50f0f09. Nothing advanced.
   - Stable latest via GitHub releases API + download.blender.org: brave 1.93.129,
     brave-origin 1.93.129, faugus 2.0.5, helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4,
     msnap 0.6.1, noctalia-greeter 1.1.0 (tag v1.1.0), protonplus 0.5.22, sdbus-cpp 2.3.1,
     vesktop 1.6.5, zen 1.21.10b, blender 5.2.0. gcc16 = 16-20260801 snapshot.
   - rootapp: fedora-nexus spec Version=0.9.125, template checksum 86ed0d76… matches the
     sha256 quoted in the fedora-nexus rootapp.spec Source0 comment. VERIFIED.
   - NO package needed a bump — all up to date, zero churn (matches the fact that the
     17:45 check-updates run produced no commit).
3. **Distfiles reachability**: curl -sI on every external distfile returned 200
   (brave deb, brave-origin zip, vesktop deb, helium tar.xz, heroic tar.xz, blender tar.xz,
   zen tar.xz + raw LICENSE, rootapp AppImage).
4. **Checksum verification by download+sha256sum**: brave-origin ad79b97… ✓,
   brave-browser 7ceca77… ✓, msnap bfc2ca74… ✓, libspng 47ec02be… ✓, zen 6a134bef… ✓
   — all match the templates exactly.
5. **Release consistency**: programmatic template-vs-rolling check — every expected
   `<pkg>-<ver>_<rev>.x86_64.xbps` present (all 27 assets incl. gcc16 subpkgs).
6. **Install-test sweep** in ghcr.io/void-linux/void-glibc-full with a local repo of the
   signed rolling assets (xbps-rindex + public.pem trusted) — ALL 19 packages installed
   cleanly, no failed to resolve transaction / missing-shlib:
   blender-bin, brave-browser, brave-origin-bin, faugus-launcher, gcc16(+subpkgs),
   helium-browser-bin, heroic-games-launcher, libspng, msnap, niri-git, noctalia,
   noctalia-greeter, protonplus, quickshell-git, rootapp, sdbus-cpp, vesktop,
   xwayland-satellite-git, zen-browser.
   Smoke: msnap --version=0.6.1, niri --version=26.04, gcc16 compile+link+run OK,
   g++16 OK. Chromium/Electron apps (brave/zen/heroic/vesktop/rootapp) launch into their
   sandbox check only — container root limitation, installation+launch path verified.
7. **Dependency deep audit** — reconciled against void-packages srcpkgs (prior run) and
   this run's clean install results; no missing/false depends found, no changes needed.
   Details in final summary table.
8. **Issues/PRs**: none open.
9. **README inventory** consistent with srcpkgs (gcc16 subpkg symlinks excluded; 19 cols).

## Final state
- Working tree clean (no template changes — everything was already current).
- No builds triggered (nothing changed).
- No churn, no version/revision bumps pushed.