# void-nexus relay state

Status: complete
run_id: 32206922633

## THIS RUN (32206922633, 2026-08-19)
### no failed builds; no open issues; no open PRs (gh issue list / gh pr list empty)
### noctalia upstream moved -> genuine bump pushed + built + install-tested
- Upstream HEAD moved fa1a968 -> 26e99a2 -> f7b0a4a (very active upstream;
  commit-per-~10min observed). ls-remote confirmed f7b0a4a still HEAD at build
  time (no further move -> no churn).
- Commit 866c9a9 "fix(autonomous): noctalia: bump to upstream HEAD f7b0a4a
  (plugin auto-update + control-centre user fixes)" pushed to main:
  _commit=f7b0a4aa258e4976b307a9b5d8ee4c0aa1508e0e, _shortcommit=f7b0a4a,
  _gitdate=20260819020127, checksum=fcfe9a6916... (download-verified).
  Git identity verified: opencode-agent[bot] <41898282+...@users.noreply.
  github.com>; no co-authored trailer.
- Build run 32207916411 SUCCESS; rolling now carries
  noctalia-5.0.0+git20260819020127.f7b0a4a_1.x86_64.xbps (old fa1a968 asset
  auto-purged, verified in asset list). Rolling still 27/27 = 19 main + 8
  gcc16 subpackages; 0 missing, 0 strays.

### Version accuracy sweep - ALL 19 re-verified THIS RUN by fresh download + sha256
| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (blender.org + application.ini "Blender 5.2.0 LTS") | up-to-date (96f6c181 verified) |
| brave-browser | 1.93.136 | v1.93.136 (releases/latest) | up-to-date (9739e5aa verified) |
| brave-origin-bin | 1.93.136 | 1.93.136 (zip asset) | up-to-date (0e272651 verified) |
| faugus-launcher | 2.1.0 | 2.1.0 (releases/latest) | up-to-date (a6956e5b verified) |
| gcc16 | 16.2.0 | 16.2.0 stable (Phoronix/GNU releases dir) | up-to-date (all 6 distfiles verified) |
| helium-browser-bin | 0.15.5.1 | 0.15.5.1 (releases/latest) | up-to-date (f34a1ee1 verified) |
| heroic-games-launcher | 2.22.1 | v2.22.1; app.asar package.json 2.22.1 | up-to-date (8a20af7b verified) |
| libspng | 0.7.4_2 | v0.7.4 (releases/latest) | up-to-date (47ec02be verified) |
| msnap | 0.6.1_3 | v0.6.1 (releases/latest) | up-to-date (bfc2ca74 verified) |
| niri-git | 26.04+git20260818151549.e9b215f_1 | HEAD == e9b215f (ls-remote) | up-to-date (8771f10f verified) |
| noctalia | 5.0.0+git20260819020127.f7b0a4a_1 | HEAD == f7b0a4a (ls-remote) | UPDATED (fcfe9a69 verified) |
| noctalia-greeter | 1.2.1_2 | tag v1.2.1 | up-to-date (1d1c4349 verified) |
| protonplus | 0.6.4_1 | v0.6.4 (tags); meson 0.6.4 | up-to-date (46665282 verified) |
| quickshell-git | 0.3.0+git20260816072204.532a060_1 | HEAD == 532a060 (ls-remote) | up-to-date (85e8effc verified) |
| rootapp | 0.9.126_1 | 0.9.126 (AppImage torn: X-AppImage-Version, NuSpec 0.9.126, fedora spec 0.9.126) | up-to-date (0736926f verified) |
| sdbus-cpp | 2.3.1_2 | v2.3.1 (tags); Void sdbus-c++ 2.3.1_1 | up-to-date |
| vesktop | 1.6.7_1 | v1.6.7 (releases/latest + deb control) | up-to-date (0204a3fc verified) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f_1 | HEAD == 3bc915f (ls-remote) | up-to-date (0ae2ac22 verified) |
| zen-browser | 1.21.14b_1 | 1.21.14b (releases/latest + application.ini) | up-to-date (b3a5bb78 + c76f740d verified) |

### Dependency audit - THIS RUN (tear-every-package)
| package | upstream deps found | in template | status |
|---------|--------------------|-------------|--------|
| noctalia f7b0a4a | 37 meson dependency() calls (cairo epoxy egl glesv2 fontconfig freetype2 gio glib gobject harfbuzz jemalloc libcurl libical libjxl libjxl_threads libpipewire libqalculate librsvg libsecret libsodium libwebp libxml md4c nlohmann_json pango pangocairo pangoft2 polkit sdbus-c sndfile tomlplusplus wayland wayland-egl wayland-protocols wireplumber xkbcommon) == makedepends | match (all 37) | deps-verified (high) |
| faugus-launcher 2.1.0 | source re-torn: Python imports gi.repository (Gtk/Gdk/GLib/Gio/GObject/GdkPixbuf/Pango/Adw/Manette/GLibUnix), vdf, requests, PIL, dbus, psutil; icoextract invoked as subprocess | depends has python3-gobject python3-requests python3-Pillow python3-vdf python3-psutil python3-dbus python3-icoextract gtk4 libadwaita libmanette | match |
| vesktop 1.6.7 | deb control Depends == template depends (gtk+3 libnotify nss libXScrnSaver libXtst xdg-utils at-spi2-core util-linux-libs libsecret); ldd verified | match | deps-verified |
| msnap 0.6.1 | cli/msnap: grim slurp wl-copy notify-send ffmpeg gpu-screen-recorder jq xdg-open curl; README dep table (grim slurp wayfreeze satty gpu-screen-recorder); wayfreeze/satty optional | bash grim slurp wl-clipboard libnotify gpu-screen-recorder ffmpeg jq xdg-utils | match |
| libspng 0.7.4 | meson: dependency(zlib) w/ miniz fallback | zlib makedepends | match |
| protonplus 0.6.4 | vala imports: Adw AppStream Gee Gtk + Json/Archive/Soup/Cairo/Notify usage; sdl3 dependency in meson | gtk4 json-glib libadwaita libarchive libgee libsoup3 SDL3 libnotify cairo AppStream | match (all exist in Void, verified) |
| noctalia-greeter 1.2.1 | meson: wayland-client/protos xkbcommon freetype fontconfig cairo ft pangocairo pangoft2 librsvg glib gobject gio wayland-server tomlplusplus nlohmann wlroots-0.20 libinput egl/glesv2(epoxy) + stb header | makedepends match; depends greetd dbus polkit bash | match |
| helium-browser-bin | ldd (87 libs): nss gtk+3 alsa cups dbus X11 Xkbcommon gbm cairo pango udev etc -> all under template depends | depends gtk+3 nss alsa-lib xdg-utils libXScrnSaver cups libgcrypt ... | match |
| heroic-games-launcher | ldd: standard Electron set under depends (nss atk alsa libcups libdrm libgbm X* gtk+3 libxcb) | match | deps-verified |
| rootapp | AppImage squashfs-root: ldd only glibc/gcc base (self-contained .NET-style, bundled libSkiaSharp/libuiohook) | depends gtk+3 nss libnotify alsa-lib xdg-utils at-spi2-core icu xorg-server-xwayland | match |
| blender-bin | bundles all libs (OpenImageIO/OpenEXR/etc), allow_unknown_shlibs=yes; ldd no missing system libs | self-contained | match |
| niri-git / quickshell-git / xwayland-satellite-git | Cargo.toml / CMakeLists (Qt6 Gui Qml Quick QuickControls2 Widgets ShaderTools + jemalloc) == makedepends (carryover, versions unchanged) | match | deps-verified |
| gcc16 | 6 distfiles (gcc gmp mpfr mpc isl gentoo patches) all verified; isl from downloads.sourceforge.net/libisl/ (NOT /project/libisl/ which 404s) | match | deps-verified |
| brave-browser / brave-origin-bin | deb control Depends (ca-certificates fonts libasound libatk libgtk-3 libnspr libnss libvulkan xdg-utils wget etc) covered by noverifyrdeps/shlib resolution + baseline | match | deps-verified |
| sdbus-cpp | metapackage: sdbus-c++>=2.3.1_1; Void ships sdbus-c++-2.3.1_1 | match | deps-verified |
| zen-browser | application.ini Version=1.21.14b; standard Mozilla deps (nss nspr libnotify libavcodec xdg-utils) | match | deps-verified |

### Install-self-test - THIS RUN (clean ghcr.io/void-linux/void-glibc-full containers, local repo, fresh xbps-rindex; all 27 xbps from rolling download + sig2)
| package | result |
|---------|--------|
| noctalia f7b0a4a (NEW binary) | installs clean; pulls libstdc++16 (rpath /opt/gcc16/lib64); smoke "noctalia v5.0.0". PASS x2 (incl. FRESH-CHECKER) |
| vesktop 1.6.7 | installs clean; smoke "Vesktop v1.6.7" (--no-sandbox). PASS |
| brave-browser 1.93.136 | installs clean (+bash for wrapper); smoke "Brave Browser 151.1.93.136". PASS |
| brave-origin-bin 1.93.136 | installs clean; smoke "Brave Origin 151.1.93.136 unknown". PASS |
| helium-browser-bin 0.15.5.1 | installs clean; smoke "Helium 0.15.5.1 (Chromium 151.0.7922.137)". PASS |
| zen-browser 1.21.14b | installs clean; smoke "Mozilla Zen 1.21.14b" (sandbox EPERM = container). PASS |
| heroic-games-launcher 2.22.1 | installs clean; smoke segfault (Electron no X/D-Bus/seat in container - known, prior run same). PASS (install) |
| rootapp 0.9.126 | installs clean; AppRun/rootapp runs (info: Program[0]). PASS |
| blender-bin 5.2.0 | installs clean; smoke "Blender 5.2.0 LTS". PASS |
| niri-git e9b215f | installs clean (pulls xwayland-satellite-git); smoke "niri 26.04". PASS |
| xwayland-satellite-git 3bc915f | installs clean; binary starts (INFO Starting ... version 0.8.2). PASS |
| quickshell-git | installs clean. PASS |
| faugus-launcher 2.1.0 | installs clean; launches (GTK init warnings only). PASS |
| msnap 0.6.1 | installs clean; smoke "msnap - screenshot and screencast tool for mangowm". PASS |
| noctalia-greeter | installs clean; 6 binaries present incl. noctalia-greeter, -compositor, -session. PASS |
| libspng / sdbus-cpp / gcc16 | install clean; gcc16 smoke "gcc16 (Void GCC 16.2.0) 16.2.0"; sdbus-cpp pulls sdbus-c++ 2.3.1_1. PASS |
- gcc16 full-chain test needed its 4 -devel subpackages in local repo; once
  present installs clean (expected; not a defect).
- FRESH-CHECKER pass: brand-new container + freshly indexed repo, noctalia
  f7b0a4a installs clean + smoke PASS.

## Prior run records (32157553813) - key facts still true
- niri-git/vesktop/noctalia(356a41a) bump commit 0fab258 verified genuine and
  install-tested. vesktop 1.6.7 arm64 deb URL 200; arm64 checksum var dead
  code (x86_64-only repo), documented gap.
- gcc16 16.2.0 stable upgrade install-tested; rolling carries gcc16-16.2.0_1
  + 8 subpackages.
- Auto-updater corruption incident (429/503 pages hashed, run 32042444124 ->
  build failure 32042579667) repaired in commit 0174de8; root-cause fix is
  the check-updates.yml change below (still pending).

## Run context
- 2026-08-19 01:55-03:05 UTC run on main (HEAD 866c9a9, RUN_ID 32206922633).
  Prior relay (32157553813) had status complete; only actionable item was the
  noctalia HEAD move (upstream very active).
- No open issues, no open PRs (gh issue list / gh pr list).
- README package table (19) consistent with srcpkgs inventory (lib*16 are
  gcc16 subpackage symlinks).

## PENDING ACTION - check-updates.yml 429/503 fix (blocked, 9th consecutive run)
- fetch_file still uses `curl -sSL` (no -f): HTTP >=400 error pages can be
  hashed into templates during rate-limit windows (see incident above). Fix:
  `curl -fsSL` + magic-byte rejection for .tar.gz/.tar.xz/.tar.bz2/.deb
  distfiles. Combined patch (with build.yml race fix) preserved at
  /tmp/opencode/workflow-fix.patch (195 lines; applies cleanly to current
  main). Local commits f92e2c7 / 52489a3 PUSH REJECTED: token is a GitHub
  App installation token WITHOUT `workflows` permission ("refusing to allow a
  GitHub App to create or update workflow ... without `workflows`
  permission"); reverted with `git reset --hard`. MUST be applied by a token
  with workflows write before the next scheduled updater (~06:00 UTC).

## PENDING ACTION - build.yml race fix (blocked, 9th consecutive run)
- Rewrite upload step to baseline from CURRENT rolling (`gh release download
  rolling -p "*.xbps"`), overlay only TARGET_PKG + subpackages, purge only
  those stale assets via `purge_stale_assets()` (jq regex
  `^${base}-[^-]+\.x86_64\.xbps(\.sig2)?$`); added jq to install deps.
  Same workflows-permission blocker. In combined workflow-fix.patch.

## not verified / gaps
- GUI/Electron smokes limited to version/launch in headless container (no
  X/D-Bus/seat/sandbox); --no-sandbox required as root. heroic smoke segfault
  is the Electron-no-seat container artifact (installs clean).
- zen/vesktop aarch64 branches not install-tested (x86_64 only; aarch64
  _deb_checksum vars are dead code, global checksum is the x86_64 value).
- Real-device launch of niri/xwayland-satellite/noctalia needs a display.
- build.yml race fix and check-updates fix remain unapplied (token lacks
  workflows write). Noctalia HEAD is moving fast; auto-updater will handle.

## Final state
- Working tree clean at end (git status --porcelain empty), HEAD ==
  origin/main (866c9a9).
- noctalia f7b0a4a (commit 866c9a9, build 32207916411 SUCCESS): binary
  published to rolling, install-tested + fresh-checker passed.
- All 19 packages verified current with download-verified checksums; rolling
  27/27 coherent (19 main + 8 gcc16 subpackages), 0 missing, 0 strays.