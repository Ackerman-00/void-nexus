# void-nexus relay state

run_id: 34185453794
status: complete

## THIS RUN (2026-09-08 ~04:26-04:40 UTC) - FULL SWEEP, ALL CURRENT, 6 INSTALL-TESTS PASS

### Pickup
- Prior relay (run 34184855706) status complete, RELAY=false: no unfinished work. Started fresh full sweep.
- At start: latest Build Nexus XBPS 34186314922 SUCCESS, latest Check and Update 34184796333 SUCCESS. Zero open issues, zero open PRs, no `autonomous run failed:*` issue. Nothing to triage.
- NOTE: repo history here is squashed commits, so NO-CHURN git-log check falls back to rolling-release comparison (authoritative for "what got out").

### Key finding: everything current, zero template edits needed
- All 19 packaged versions match live upstream (gh api + curl -sI, this run). Rolling assets match every template version_revision. No failed builds, no issues/PRs.
- Per NO-CHURN rules: zero template edits pushed by this run.

### Version Sweep - ALL 19 packages verified THIS RUN (live gh api + curl)
| package | packaged version | upstream latest | verification method | status |
|---------|------------------|-----------------|---------------------|--------|
| blender-bin | 5.2.1_1 | 5.2.1 | curl -sI 200 on Blender5.2/blender-5.2.1 tarball | up-to-date |
| brave-browser | 1.94.121_1 | v1.94.121 | gh api brave/brave-browser releases/latest | up-to-date |
| brave-origin-bin | 1.94.121_1 | v1.94.121 | same release as brave-browser | up-to-date |
| faugus-launcher | 2.2.2_1 | 2.2.2 | gh api Faugus/faugus-launcher releases/latest | up-to-date |
| gcc16 (+8 lib subpkgs) | 16.2.0_1 | 16.2.0 | ftp.gnu.org listing max gcc-16.2.0 | up-to-date |
| helium-browser-bin | 0.16.5.1_1 | 0.16.5.1 | gh api imputnet/helium-linux releases/latest | up-to-date |
| heroic-games-launcher | 2.22.1_1 | v2.22.1 | gh api Heroic-Games-Launcher/HeroicGamesLauncher releases/latest | up-to-date |
| libspng | 0.7.4_2 | v0.7.4 | gh api randy408/libspng releases/latest | up-to-date |
| msnap | 0.6.1_4 | v0.6.1 | gh api xtheeq/msnap releases/latest | up-to-date |
| niri-git | 26.04+git20260821184624.dd75865_1 | HEAD dd75865 2026-08-21T18:46:24Z | gh api niri-wm/niri commits/main sha+date match | up-to-date |
| noctalia | 5.0.1+git20260908021120.7e919a0_1 | HEAD 7e919a0 2026-09-08T02:11:20Z | gh api noctalia-dev/noctalia commits/main exact match | up-to-date |
| noctalia-greeter | 1.3.1_1 | v1.3.1 | gh api noctalia-dev/noctalia-greeter tags[0] | up-to-date |
| protonplus | 0.6.6_1 | v0.6.6 | gh api Vysp3r/ProtonPlus releases/latest | up-to-date |
| quickshell-git | 0.3.1+git20260829091143.2d3b3e9_1 | HEAD 2d3b3e9 2026-08-29T09:11:43Z | gh api quickshell-mirror/quickshell commits/master sha+date match | up-to-date |
| rootapp | 0.9.129_1 | 0.9.129 | installer.rootapp.com 200, Last-Modified Wed 02 Sep 2026 unchanged = same binary | up-to-date |
| sdbus-cpp | 2.3.1_2 | v2.3.1 | gh api Kistler-Group/sdbus-cpp releases/latest | up-to-date |
| vesktop | 1.6.7_1 | v1.6.7 | gh api Vencord/Vesktop releases/latest | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260908031128.7f848f5_1 | HEAD 7f848f5 2026-09-08T03:11:28Z | gh api Supreeeme/xwayland-satellite commits/main sha+date match | up-to-date |
| zen-browser | 1.22b_1 | 1.22b | gh api zen-browser/desktop releases/latest | up-to-date |

Rolling: all .xbps assets present and match template version_revision. No-churn guard PASS: zero bumps pushed by this run.

### Dependency Audit - ALL 19 packages (no upstream changed since prior full re-tear audit, carry-forward)
- No package's upstream changed since the prior run's re-tear audit (versions identical, rootapp binary Last-Modified unchanged), so per RE-TEAR RULE no re-derivation is voided/required. Prior run diffed noctalia meson deps + xwayland Cargo.toml/lock identical across bumps.
| package | upstream deps found | in template | missing/added | extra/dropped | status |
|---------|---------------------|-------------|---------------|---------------|--------|
| blender-bin | prebuilt tarball bundled libs | fetch style, hostmakedepends tar xz | none | none | deps-verified |
| brave-browser | chromium shlibs + bash wrappers | bash | none | none | deps-verified |
| brave-origin-bin | chromium bundle + launcher | bash alsa-lib gtk+3 libXScrnSaver nss | none | none | deps-verified |
| faugus-launcher | meson python gi/requests/pillow stack | meson pkg-config gettext glib-devel gtk-update-icon-cache; python3-gobject stack | none | none | deps-verified |
| gcc16 | gmp/mpfr/mpc/isl vendored + patchset | vendored + zlib-devel libzstd-devel; checkdepends dejagnu | none | none | deps-verified |
| helium-browser-bin | prebuilt tarball chromium 152 class | chromium-bundle depends set | none | none | deps-verified |
| heroic-games-launcher | prebuilt electron bundle | desktop-file-utils nss atk alsa-lib cups drm gbm + X libs | none | none | deps-verified |
| libspng | meson zlib | meson style, zlib-devel | none | none | deps-verified |
| msnap | bash script wayland tools | bash grim slurp wl-clipboard libnotify gpu-screen-recorder ffmpeg jq xdg-utils | none | none | deps-verified |
| niri-git | Cargo wayland stack | pkg-config + cairo eudev glib display-info gbm input seat xkbcommon MesaLib pango pipewire pixman clang19; depends wayland xwayland-satellite-git | none | none | deps-verified |
| noctalia | meson dependency() re-derived prior run from 7e919a0, identical vs 4437179 (39 entries) | full makedepends incl gcc16 libstdc++16-devel sdbus-c++-devel tomlplusplus json-c++ stb jemalloc; depends libstdc++16 | none | none | deps-verified |
| noctalia-greeter | meson wayland/wlroots stack | meson ninja pkg-config wayland-devel; wayland stack; depends greetd dbus polkit bash | none | none | deps-verified |
| protonplus | meson sdl3 gtk4 glib adwaita gee soup3 archive notify cairo appstream | meson vala glib gtk4 adwaita archive gee soup3 SDL3 cairo notify + runtime depends | none | none | deps-verified |
| quickshell-git | CMake Qt6 stack | qt6 devels drm xcb cli11 wayland pipewire polkit glib pam jemalloc | none | none | deps-verified |
| rootapp | AppImage Avalonia/.NET bundled (binary unchanged, Last-Modified 02 Sep 2026) | gtk+3 nss libnotify alsa-lib xdg-utils at-spi2-core icu xorg-server-xwayland | none | none | deps-verified |
| sdbus-cpp | metapackage | sdbus-c++>=2.3.1 (resolution PROVEN this run: install pulled sdbus-c++ from Void, xbps-query shows run_depends sdbus-c++>=2.3.1_1) | none | none | deps-verified |
| vesktop | prebuilt electron bundle | gtk+3 notify nss XScrnSaver Xtst xdg-utils at-spi2-core util-linux-libs libsecret | none | none | deps-verified |
| xwayland-satellite-git | Cargo.toml + Cargo.lock re-derived prior run from 7f848f5, identical vs ea474b9 | libxcb-devel xcb-util-cursor-devel; depends xorg-server-xwayland | none | none | deps-verified |
| zen-browser | prebuilt firefox tarball 1.22b | desktop-file-utils hicolor-icon-theme notify libavcodec xdg-utils nss nspr | none | none | deps-verified |

Confidence: high for all rows (fresh-container installs this run prove dep resolution for 5 packages incl. same-repo chains niri->xwayland, noctalia->libstdc++16 class via gcc16).

### Toolchain evidence (REAL runs this run)
- **xlint PASS**: REAL xlint (xtools in fresh ghcr.io/void-linux/void-glibc-full:latest container): all templates CLEAN except the 4 known intentional git-snapshot version convention flags (niri-git/noctalia/quickshell-git/xwayland-satellite-git line 10 "version must not contain _", documented in-template, production builds SUCCESS).
- **bash -n PASS**: all srcpkgs/*/template syntax OK on runner. **yaml PASS**: all 4 workflows safe_load clean. README mentions all 19 packages, no drift.
- **xbps-src PASS**: production CI authoritative -- Build Nexus XBPS 34186314922 SUCCESS (latest build).
- **xbps-install PASS**: 6 fresh-container installs this run (see install-test table) incl. fresh-checker re-run.
- **sha256sum / curl -sI PASS**: blender distfile 200; rootapp 200 + Last-Modified unchanged; gcc max 16.2.0.

### Install-Verification Sweep - THIS RUN - xbps install test (each row = FRESH void-glibc-full container, DOCKER BATTLE TEST, local repo via xbps-rindex -f -a, key trust via public.pem copied to /var/db/xbps/keys; recipe requires xbps-install -S sync first)
| package | build | xbps install test | smoke test | status |
|---------|-------|-------------------|------------|--------|
| sdbus-cpp 2.3.1_2 | rolling (pre-existing) | PASS | metapackage, xbps-query shows run_depends sdbus-c++>=2.3.1_1 | installable |
| msnap 0.6.1_4 | rolling (pre-existing) | PASS (169 pkgs) | msnap --help exit 0 | installable |
| niri-git dd75865_1 | rolling (pre-existing) | PASS (131 pkgs, with rolling xwayland-satellite-git in repo) | niri --version "niri 26.04 (unknown commit)" exit 0; ldd REAL 0 not found (with bash) | installable |
| brave-origin-bin 1.94.121_1 | rolling (pre-existing) | PASS (126 pkgs) | brave-origin --version "Brave Origin 152.1.94.121 unknown" exit 0; ldd 0 not found | installable |
| gcc16 + libstdc++16 16.2.0_1 | rolling (pre-existing) | PASS (25 pkgs, with full gcc16 family in repo) | gcc16 --version "gcc16 (Void GCC 16.2.0) 16.2.0" exit 0; ldd 0 not found | installable |
| sdbus-cpp 2.3.1_2 (fresh-checker re-run, separate fresh container) | rolling (pre-existing) | PASS (3 pkgs) | xbps-query confirms installed | installable |

Coverage: this run tested the 5 packages left uncovered by the prior run (brave-origin-bin, gcc16 family, msnap, niri-git, sdbus-cpp) -- combined with the prior run's 14, ALL 19 binaries now install-tested across the two runs with zero binary changes in between. No package's binary changed since its last test.
- TRAPS FOUND (test-harness, not repo bugs): (1) current void-glibc-full image ships NO xbps-key binary and NO bash -- key trust via `cp public.pem /var/db/xbps/keys/nexus.plist` works (sig2 verified, installs succeed); ldd requires installing bash first. (2) image's bundled Void repo data is STALE -- always `xbps-install -S` first in fresh containers. (3) same-repo deps (niri->xwayland-satellite-git, gcc16 family splits) REQUIRE the full rolling set in the local test repo -- a subset repo gives bogus MISSING/unresolved errors; first niri attempt failed exactly this way, retest with xwayland present PASS. (4) runner has no xbps-rindex -- index inside the container.

### Adversarial audit (contrarian pass, all checked with commands)
1. "zero-change run could be missing a stale upstream" -> counter: every version re-queried live this run via gh api (14 release/tag/commit endpoints) + curl -sI (blender, rootapp) + ftp.gnu.org listing (gcc); all match templates exactly. Verdict: genuinely current, not a skipped check.
2. "niri/gcc first-attempt install failures could be packaging bugs" -> counter: both failures were MISSING/unresolved same-repo deps absent from my subset test repo (xwayland-satellite-git, gcc16 splits); with the full set present both install + run clean. Void users get the full repo per README. Verdict: test-harness incompleteness, not a bug.
3. "carry-forward dep audit could hide a changed upstream" -> counter: versions byte-identical to prior run's re-tear audit inputs; rootapp binary Last-Modified unchanged (same bytes). Verdict: no voided derivation.

### Issues / PRs
- Zero open issues, zero open PRs at start and end (`gh issue list`, `gh pr list` empty). No `autonomous run failed:*` issue exists. Nothing to reply/merge/close.

### Pre-completion gate
1. git status clean: PASS (only .opencode-relay.md modified; test artifacts in /tmp only).
2. Every touched package install-tested in CLEAN container incl. fresh-checker re-run: PASS (zero template edits; 5 installs + 1 checker).
3. Coverage ledger updated; every changed binary tested: PASS (no binary changed since last test; the 5 previously-uncovered packages tested this run).
4. Dependency audit covers 100% incl. re-derivation for changed upstreams: PASS (19 rows, none voided).
5. Every claim backed by command output: PASS (gh api outputs, docker/xbps logs, xlint output above).
6. Gaps named: full teardown-sweep.py not re-run (targeted live checks + prior re-tear evidence suffice); PR/issue self-test N/A (zero open); GUI apps smoke-tested to version/--help only (headless).
7. Relay updated+pushed with run_id: 34185453794 + status: complete: DONE (this commit).
