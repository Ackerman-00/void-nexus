# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-04 ~07:25 UTC run (opencode-schedule, no relay input).
- Previous run: 2026-08-04 ~07:06 UTC (status: complete).

## Completed this run
1. **Failed-build triage**: All recent builds (last 10) are successful. No failures to address.
2. **Version inventory sweep (all 19 packages) — web/API verified current.**
   - Git snapshots: niri feb3e43 (up to date), noctalia e41c994 (up to date), quickshell 28771c7 (up to date), xwayland-satellite 8d135d3 (up to date).
   - Stable latest all confirmed: blender 5.2.0, brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5, helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4, msnap 0.6.1, noctalia-greeter 1.1.0, protonplus 0.5.22, sdbus-cpp 2.3.1, vesktop 1.6.5, zen 1.21.10b, rootapp 0.9.125, gcc16 16.1.1+20260801.
3. **Release consistency**: All 27 rolling release assets (19 packages + 8 gcc16 subpackages) match templates.
4. **Install-test sweep** (full coverage in ghcr.io/void-linux/void-glibc-full):
   - blender-bin: install OK
   - brave-browser: install OK
   - brave-origin-bin: install OK
   - faugus-launcher: install OK
   - helium-browser-bin: install OK
   - heroic-games-launcher: install OK
   - libspng: install OK
   - msnap: install OK
   - noctalia: install OK
   - noctalia-greeter: install OK
   - protonplus: install OK
   - quickshell-git: install OK
   - rootapp: install OK
   - sdbus-cpp: install OK
   - vesktop: install OK
   - xwayland-satellite-git: install OK
   - zen-browser: install OK
   - gcc16 + all subpackages: install OK
   - niri-git: install OK
   - Fresh-checker re-run: 4 packages (rootapp, vesktop, gcc16, zen-browser) installed on clean container — all pass.
5. **Issues/PRs**: no open issues/PRs.
6. **README inventory**: 19 packages listed, 19 real template directories — consistent.
7. **Churn check**: No duplicate version bumps detected. No auto-update commits touching same package/version repeatedly.

## Pending
- Nothing. All work complete.

## Final state
- Working tree clean (`git status --porcelain` empty).
- No builds triggered (nothing needed updating).
- No churn.
