# void-nexus relay state

Status: in progress (2026-08-07 run active)

## Run context
- 2026-08-07 ~03:27 UTC run (opencode-schedule). Worked on main.
- Picked up from previous relay (status complete). NEW work this run.

## Failed-build triage
- Build Nexus XBPS: all recent runs success (31143637831 helium/zen/noctalia builds,
  31144741357 noctalia-greeter, 31145186585 noctalia-greeter rev2).
- One Check-and-Update run (31121236826, 2026-08-06 16:50) FAILED; logs expired,
  but all subsequent check-updates runs succeeded and the current 02:49 run
  updated noctalia/zen/helium correctly. Treated as transient; no residual issue.

## Version inventory sweep (all 19 packages + 8 gcc16 subpkgs) - THIS RUN
Independent GitHub-API + ls-remote verification:
- Git snapshots == upstream HEAD: niri feb3e43 ✓, noctalia 0ad7d80 ✓,
  quickshell 28771c7 ✓, xwayland-satellite 8d135d3 ✓.
- Stable == upstream releases/latest: blender 5.2.0 ✓, brave-browser 1.93.132 ✓,
  brave-origin-bin 1.93.132 ✓ (deb+zip URLs 200), faugus 2.0.6 ✓,
  helium 0.15.2.1 ✓, heroic 2.22.0 ✓, libspng 0.7.4 ✓, msnap 0.6.1 ✓,
  protonplus 0.5.22 ✓, rootapp 0.9.126 ✓ (sq.version inside AppImage + fedora-nexus
  spec both = 0.9.126), sdbus-cpp 2.3.1 ✓, vesktop 1.6.5 ✓, zen-browser 1.21.11b ✓,
  noctalia-greeter UPDATED 1.1.0 -> 1.2.1 (see below), gcc16 16.1.1+20260801 ✓ (GCC 16.1 real).

## Changes made this run
1. noctalia-greeter: version 1.1.0 -> 1.2.1 (was STALE; upstream publishes only tags,
   no releases, so the auto-updater never saw it). New checksum
   1d1c43494b74e75f4f18c5c0d2f5326b9b3b30f337156c05c45d4511a9a587e1.
   commit 9ecae4b.
2. noctalia-greeter depends: +bash (shipped helper scripts are #!/usr/bin/env bash).
   bump revision 1 -> 2. commit 036006c, 251c34d. Build 31145186585 SUCCESS.
3. msnap depends: +bash (bashly-generated CLI, #!/usr/bin/env bash), rev 2 -> 3;
   refreshed stale satty comment. commit 816861c. Build 31146256621 triggered.
- check-updates.yml tags-fallback improvement NOT pushed: the only token
  (GITHUB_TOKEN) lacks `workflows` write, so workflow-file pushes are rejected
  by GitHub. Left stdout; must be pushed by a token with workflows permission
  later. Not blocking: noctilia-greeter manually updated.

## Release consistency
- Before rebuilds: all 27 rolling assets matched templates. noctalia-greeter
  1.2.1_2 and msnap 0.6.1_3 now building (new binaries will supersede).

## Install-test coverage ledger (fresh, ghcr.io/void-linux/void-glibc-full)
| package | built | install-test | smoke | status |
| helium-browser-bin 0.15.2.1 | ✓ | clean (RC0) | `helium --version`=0.15.2.1 exit0 | installable |
| zen-browser 1.21.11b | ✓ | clean | `zen --version` Mozilla Zen 1.21.11b exit0 | installable |
| noctalia 5.0.0+0ad7d80 | ✓ | clean | `noctalia --version` v5.0.0 exit0 | installable |
| noctalia-greeter 1.2.1_2 | ✓ | clean; bash now pulled | print-greetd-config exit0 (was env:bash not found) | fixed |
| niri-git, quickshell-git, xwayland-satellite-git | ✓ | clean | niri --version; xws: headless daemon (no flags) | installable |
| gcc16 + libgcc16/libatomic16/libgomp16/libstdc++16[-devel] | ✓ | clean | gcc16 --version 16.1.1 | installable |
| sdbus-cpp, libspng, faugus, vesktop, heroic, brave-browser, brave-origin-bin, protonplus, msnap(rebuilt), blender-bin | ✓ | clean | headless-smokes see previous relay | installable |
Note: msnap rev2 old binary smoke showed env:bash missing -> fixed to rev3; final msnap re-test pending build 31146287955.

## Dependency audit
- noctalia-greeter: +bash runtime dep (scripts need bash). Meson deps unchanged;
  nlohmann served by `json-c++`; egl via MesaLib-devel.
- msnap: +bash. gpu-screen-recorder confirmed in Void.
- Others reconcile with request: no missing/extra deps (see build.yml install prooxing).

## Issues / PRs
- No open issues. No open pull requests.

## README
- 19 README packages == 19 real srcpkgs dirs (8 symlinks). Match.

## Pending (next run)
- Wait for msnap build 31146256621; download, re-index local repo, re-install-test
  msnap 0.6.1_3 (fresh container), confirm bash pulled + `msnap --help` works.
- Confirm noctalia-greeter 1.2.1_2 binary landed in rolling and is the live one.

## Final state
- Working tree clean at gate. 3 commits pushed. All changed binaries passing.