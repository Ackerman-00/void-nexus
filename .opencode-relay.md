# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-04 ~11:04 UTC run (opencode-schedule, no relay input).
- Previous run: 2026-08-04 ~07:25 UTC (status: complete).

## Failed-build triage
- All recent "Build Nexus XBPS" and "Check and Update Package Versions" runs: SUCCESS.
- Two recent opencode-schedule runs (07:06, 07:25 UTC) were marked FAILURE only in the
  final "Clean up stray opencode/schedule branches" step, due to a raw
  `git push origin --delete` authentication failure ("Invalid username or token").
  No work was lost; the relay state was committed before that step.
- FIX applied this run: rewrote that cleanup step to use `gh api -X DELETE
  repos/${{ GITHUB_REPOSITORY }}/git/refs/heads/<ref>` with `GH_TOKEN` env, which
  authenticates reliably with GITHUB_TOKEN. Validated YAML. Committed to main as
  part of this run's `ci(autonomous)` commit.

## Version inventory sweep (all 19 packages) — verified current
- Git snapshots at upstream HEAD (via git ls-remote):
  niri feb3e43 ✓, noctalia e41c994 ✓, quickshell 28771c7 ✓,
  xwayland-satellite 8d135d3 ✓.
- Stable latest confirmed via upstream API + web search + distfile checksums:
  blender 5.2.0, brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5,
  helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4, msnap 0.6.1,
  noctalia-greeter 1.1.0, protonplus 0.5.22, rootapp 0.9.125, sdbus-cpp 2.3.1,
  vesktop 1.6.5, zen 1.21.10b, gcc16 16-20260801 (latest snapshot).
- rootapp torn apart: X-AppImage-Version=0.9.125 + sq.version 0.9.125; sha256
  86ed0d76... matches template AND fedora-nexus rootapp.spec. Current.
- Adversarial audit: brave v1.95.x tags are Nightly-channel releases (no stable
  binaries); stable release is v1.93.129, matching template. NOT a version miss.
  helium 0.15.1.1 confirmed newest via releases list (web index was stale 0.14.x).
- Every distfile URL returned 200 and every sha256 matched the template checksum
  (all 19 packages + gcc16 6 distfiles). Verified by download + sha256sum.

## Release consistency
- All 27 rolling assets (19 packages + 8 gcc16 subpackages) exactly match the
  templates' expected `<pkg>-<ver>_<rev>.x86_64.xbps`. No missing/stale binaries.

## Install-test sweep (all 19 packages in ghcr.io/void-linux/void-glibc-full)
- Main sweep + fresh-checker re-run both: INSTALL_OK for all 19 packages
  (clean transaction resolution against main Void repo).
- Smoke OK: blender-bin, brave-browser (binary is `brave-browser-stable`),
  brave-origin-bin, faugus-launcher, gcc16, helium-browser-bin, msnap (bash
  script; OK once bash installed), niri-git, noctalia, noctalia-greeter,
  quickshell-git, rootapp, zen-browser.
- Smoke requires display/daemon (nonzero headless, NOT a defect): heroic,
  protonplus, vesktop (Electron/GTK), xwayland-satellite (daemon, no --help).
- Lib-only/metapackage (no smoke): libspng, sdbus-cpp.

## Issues / PRs
- No open issues, no open PRs.

## README
- 19 packages in table = 19 real template dirs. Consistent.

## Churn / no-churn
- No version or revision bumps needed (everything current). No duplicate
  auto-update commits for the same version detected.

## Pending
- Nothing. All work complete.

## Final state
- One repo change this run: opencode-schedule.yml cleanup-step auth fix (gh api).
- Working tree clean (`git status --porcelain` empty).