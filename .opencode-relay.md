# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-06 ~04:00 UTC run (opencode-schedule, no relay input).
- Branch: worked on main. Updated noctalia to latest upstream commit.

## Failed-build triage
- Recent "Build Nexus XBPS": SUCCESS (all 10 recent runs passed).
- Recent "Check and Update Package Versions": SUCCESS every cycle.
- No failed builds requiring attention.

## Version inventory sweep (all 19 packages + 8 gcc16 subpackages)
Independent verification, all sources checked this run:
- Git snapshots @ upstream HEAD (git ls-remote): niri feb3e43 = HEAD ✓,
  noctalia 4f459ff = HEAD ✓ (UPDATED from e9db917), quickshell 28771c7 = HEAD ✓,
  xwayland-satellite 8d135d3 = HEAD ✓.
- Stable (upstream latest verified via GH API releases/latest):
  blender 5.2.0 ✓, brave-browser 1.93.132 ✓, brave-origin-bin 1.93.132 ✓,
  faugus 2.0.6 ✓, helium 0.15.1.1 ✓, heroic 2.22.0 ✓, libspng 0.7.4 ✓,
  msnap 0.6.1 ✓, noctalia-greeter 1.1.0 ✓, protonplus 0.5.22 ✓,
  rootapp 0.9.126 ✓ (matches fedora-nexus spec), sdbus-cpp 2.3.1 ✓ (matches GitHub releases/latest),
  vesktop 1.6.5 ✓, zen-browser 1.21.10b ✓, gcc16 16.1.1+20260801 ✓ (latest snapshot).

## Change made this run
- noctalia: bumped _commit from e9db917f to 4f459ffc (10 min newer commit:
  "ui(notification): use emphasized border width for critical toasts").
  Updated checksum. Build triggered (run 31070344097) and SUCCEEDED.

## Release consistency
- All 27 rolling assets (19 pkgs + gcc16 + 8 subpkgs) exactly match expected
  `<pkg>-<ver>_<rev>.x86_64.xbps`. No missing/stale binaries.

## Install-test sweep (FRESH, this run - clean ghcr.io/void-linux/void-glibc-full)
Built local repo from rolling binaries + trusted public.pem; for EACH package a fresh
container, synced main Void repo + nexus repo overlay, `xbps-install -y <pkg>`.
ALL 19 packages + gcc16: xbps-install clean (RC 0):
- Tested: blender-bin, brave-browser, brave-origin-bin, faugus-launcher, gcc16,
  helium-browser-bin, heroic-games-launcher, libspng, msnap, niri-git,
  noctalia-greeter, protonplus, quickshell-git, rootapp, sdbus-cpp, vesktop,
  xwayland-satellite-git, zen-browser, (noctalia: old binary from rolling, new one building).
- Electron apps (vesktop, zen-browser, brave-browser, heroic): correctly refuse to run
  as root (--no-sandbox required) - expected behavior, install verified.

## Dependency audit (every package reconciled this run)
- brave-browser: NO template depends, auto-resolved from shlibs ✓.
- sdbus-cpp: metapackage depends sdbus-c++>=2.3.1_1 resolves to Void pkg ✓.
- noctalia: makedepends includes json-c++ and stb - these are Void package names ✓.
- No missing/extra deps found. status: deps-verified (all).

## Issues / PRs
- No open issues. No open pull requests.

## README
- 19 README packages == 19 real srcpkgs dirs (8 gcc16 subpkgs are symlinks).
  Match verified. Nothing stale.

## Churn / no-churn
- noctalia commit bumped (genuinely new upstream commit, not a churn duplicate).
  Previous commit e9db917 was from 02:45 UTC; new 4f459ff from 02:55 UTC.
  The auto-updater missed it by 6 minutes (ran at 02:51 UTC).

## Pending
- Nothing.

## Final state
- noctalia updated + built successfully (run 31070344097).
- Working tree clean. All 19+8 packages verified installable.
