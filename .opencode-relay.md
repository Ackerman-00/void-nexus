# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-03 ~15:26 UTC run (opencode-schedule, no relay input).
- Picked up repo at 3e69aae; unshallowed history (1155 commits) for no-churn audit.

## Completed this run
1. **Failed-build triage**: All recent "Build Nexus XBPS" and "Check and Update
   Package Versions" runs succeeded. The only red run (30795597764) was an
   opencode-schedule that died on a transient 503 model-queue error (infra, not the repo).
2. **Version inventory sweep (all 19 packages) — web/API/artifact verified current.**
   - Git snapshots pinned to upstream default-branch HEAD:
     niri feb3e43, quickshell 28771c7 (mirror), xwayland-satellite 8d135d3 (main).
   - **noctalia was 1 commit stale (6c1acb5 → 50f0f09, PR #3754)**. Updated template
     commit/gitdate/checksum, pushed to main (2b9774c), built (run 30827990924 ✓),
     new binary noctalia-5.0.0+git20260803143218.50f0f09_1 uploaded.
   - Stable latest confirmed: brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5,
     helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4, msnap 0.6.1, noctalia-greeter 1.1.0,
     protonplus 0.5.22, sdbus-cpp 2.3.1, vesktop 1.6.5, zen 1.21.10b, blender 5.2.0.
   - gcc16 = latest 16-20260801 snapshot.
   - rootapp: AppImage re-downloaded + torn apart; sq.version=0.9.125,
     X-AppImage-Version=0.9.125, fedora-nexus spec 0.9.125, sha256
     86ed0d76… matches template. VERIFIED.
   - ALL distfiles return 200; ALL checksums re-verified by download+sha256sum
     (blender official .sha256, rootapp, vesktop, helium, zen, heroic, brave,
     brave-origin, faugus, libspng, msnap, noctalia-greeter, protonplus, niri,
     noctalia, quickshell, xws, gcc16 + gmp/mpfr/mpc/isl/gentoo-patchset).
3. **Release consistency**: programmatic template-vs-rolling check — 0 missing;
   every expected xbps present, incl. gcc16 subpkgs.
4. **Dependency deep audit** (16 templates sourced, tarballs extracted, isolated
   verified against void-packages srcpkgs tree). Findings were evaluated against
   "only fix what's broken + no churn": vesktop's `util-linux-libs` IS a real Void
   metapackage providing libuuid (audit flag was a false positive — install passed);
   brave-browser auto-resolves shlibs at build (install clean, deps pulled correctly);
   niri/gcc16/msnap/faugus/noctalia install clean. No dead/false deps needed removal.
5. **Install-test sweep** in ghcr.io/void-linux/void-glibc-full (local repo of rolling
   assets + sig2s + public.pem): msnap, faugus-launcher, protonplus, heroic,
   braver-browser, braver-origin, vesktop, niri-git, helium, zen, rootapp, sdbus-cpp,
   libspng, noctalia-greeter, gcc16(+glibc-devel) — ALL installed cleanly,
   no unresolved transaction / missing-shlib. gcc16 compiles+links+runs (exit 42).
6. **Issues/PRs**: none open.
7. **README inventory** consistent with srcpkgs (gcc16 subpkg symlinks excluded).

## Final state
- Working tree clean.
- One build triggered & passed: noctalia (2b9774c → run 30827990924).
- No churn: only noctalia genuinely advanced upstream.