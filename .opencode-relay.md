# void-nexus relay state

Status: complete
run_id: 31235016513

## Run context
- 2026-08-08 ~02:27 UTC run (opencode-schedule). Worked on main.
- Picked up from previous relay (run 31197337399, status complete). This run:
  re-verified every package version/checksum against upstream AND against live
  artifacts, rebuilt a local repo from FRESH release assets and re-installed all
  19 packages (fresh-checker second pass), ran the full dependency audit against
  built props.plist run_depends, re-smoked every binary, and did a security/
  adversarial audit of templates and workflows.

## Version inventory sweep (all 19 real packages + 8 gcc16 subpkgs) - THIS RUN
Verified via upstream API/ls-remote this run. niri-git was bumped to 59a1001 by
the 01:52 check-updates run (31233668174); build 31233695050 (01:53) SUCCESS;
new asset is in rolling. All other packages unchanged and still current:

| package | template version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (blender.org) | up-to-date |
| brave-browser | 1.93.134 | v1.93.134 (Release channel) | up-to-date |
| brave-origin-bin | 1.93.134 | v1.93.134 | up-to-date |
| faugus-launcher | 2.0.6 | 2.0.6 | up-to-date |
| gcc16 | 16.1.1+20260801 | snapshot live (200) | up-to-date (built) |
| helium-browser-bin | 0.15.2.1 | 0.15.2.1 | up-to-date |
| heroic-games-launcher | 2.22.0 | v2.22.0 | up-to-date |
| libspng | 0.7.4 | v0.7.4 | up-to-date |
| msnap | 0.6.1 | v0.6.1 | up-to-date |
| niri-git | 26.04+git20260807194758.59a1001 | upstream HEAD == 59a1001 | up-to-date (BUMPED this run window) |
| noctalia | 5.0.0+git20260807151429.e9230c8 | upstream HEAD == e9230c8 | up-to-date |
| noctalia-greeter | 1.2.1 | tag v1.2.1 (no releases) | up-to-date |
| protonplus | 0.5.22 | v0.5.22 | up-to-date |
| quickshell-git | 0.3.0+git20260802205341.28771c7 | mirror HEAD == 28771c7 | up-to-date |
| rootapp | 0.9.126 | 0.9.126 (sq.version inside AppImage) | up-to-date |
| sdbus-cpp | 2.3.1 | v2.3.1 (metapackage) | up-to-date |
| vesktop | 1.6.5 | v1.6.5 | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260722002452.8d135d3 | upstream HEAD == 8d135d3 | up-to-date |
| zen-browser | 1.21.12b | 1.21.12b | up-to-date |

CROSS-CHECK (artifacts torn apart): rootapp AppImage sq.version +
X-AppImage-Version == 0.9.126 (== fedora-nexus rootapp.spec); vesktop app.asar
== 1.6.5; zen application.ini == 1.21.12b; helium strings == 0.15.2.1;
brave-origin strings == 1.93.134; brave deb Version == 1.93.134; heroic release
tag v2.22.0. All 17 non-gcc distfile checksums verified against templates this
run (brave, brave-origin, vesktop, zen+license, heroic, helium, protonplus,
faugus, msnap, libspng, noctalia-greeter, niri-git, noctalia, quickshell-git,
xwayland-satellite, rootapp). gcc16 distfiles verified via URL 200 + already
built. isl-0.27 checksum 62633552... confirmed from libisl.sourceforge.io.

## Release consistency
- 27/27 rolling x86_64.xbps assets match every template's evaluated
  version/revision (verified programmatically THIS RUN against fresh
  gh release view). Old niri feb3e43 asset replaced by the 59a1001 build.
  No rebuild needed, no churn.

## Fresh-checker second pass (independent re-verification)
- Downloaded ALL 27 xbps + 27 sig2 fresh from rolling into a NEW repo dir,
  rebuilt index (`xbps-rindex -f -a` -> 27 packages registered), and re-installed
  all 19 package groups in a fresh ghcr.io/void-linux/void-glibc-full container
  using ONLY that freshly-built repo. All installed cleanly (0 errors):
  brave-browser (125 pkgs), brave-origin-bin, zen-browser, helium-browser-bin,
  heroic-games-launcher, vesktop, blender-bin, gcc16 (12 pkgs incl. subpkgs),
  rootapp, libspng, sdbus-cpp, noctalia-greeter (66 pkgs), protonplus (144 pkgs),
  msnap, faugus-launcher, niri-git, noctalia, quickshell-git,
  xwayland-satellite-git (already present as niri dep - expected).
- Key chain re-verified: xbps accepted the repo signature using the committed
  signing key (key file used == signing-key.plist byte-for-byte; public.pem
  sha256-DER fingerprint 44:bd:4b:...:24:41 matches the key filename). This is
  authoritative proof the released repo is signed by the committed key.

## Install-test + smoke coverage ledger (this run)
| package | xbps install | smoke result | status |
|---------|--------------|--------------|--------|
| brave-browser 1.93.134 | clean | `Brave Browser 151.1.93.134` | installable |
| brave-origin-bin 1.93.134 | clean | `Brave Origin 151.1.93.134` | installable |
| zen-browser 1.21.12b | clean | `Mozilla Zen 1.21.12b` | installable |
| helium-browser-bin 0.15.2.1 | clean | `Helium 0.15.2.1` | installable |
| vesktop 1.6.5 | clean | `Vesktop v1.6.5` (--no-sandbox) | installable |
| rootapp 0.9.126 | clean | app launches (headless) | installable |
| niri-git 59a1001 | clean | `niri 26.04` | installable |
| noctalia e9230c8 | clean | help text OK | installable |
| quickshell-git | clean | `Quickshell 0.3.0` | installable |
| xwayland-satellite-git | clean | starts, needs XDG_RUNTIME_DIR (headless) | installable |
| faugus-launcher 2.0.6 | clean | runs headless | installable |
| msnap 0.6.1 | clean | `msnap --help` OK | installable |
| blender-bin 5.2.0 | clean | `Blender 5.2.0 LTS` | installable |
| gcc16 16.1.1+20260801 | clean | `gcc16 --version` OK | installable |
| protonplus 0.5.22 | clean | loads (needs network/display headless) | installable |
| heroic-games-launcher 2.22.0 | clean | needs X server (headless) | installable |
| libspng 0.7.4 | clean | lib present | installable |
| sdbus-cpp 2.3.1 | clean | headers present | installable |
| noctalia-greeter 1.2.1 | clean | greeter needs display | installable |

Headless smokes that require a display/X (heroic, noctalia-greeter) or
network-on-start (protonplus) are environment limitations, NOT packaging bugs.

## Dependency audit (this run, from built props.plist)
All template `depends` verified present in the BUILT package run_depends (no
dead/missing/extra deps):
- faugus-launcher: template 13 deps == built run_depends 13 (python3 stack +
  gtk4 libadwaita libmanette polkit xdg-utils).
- msnap: 9 == 9 (bash grim slurp wl-clipboard libnotify gpu-screen-recorder
  ffmpeg jq xdg-utils).
- zen-browser: 7 template deps all in built (30 total incl. shlib-auto).
- vesktop: 9 all present (31 total incl. auto).
- rootapp: 8 all present (17 total incl. auto libX11/libXinerama/libXt/libXtst/
  fontconfig/libxkbcommon - correct for an Electron/Avalonia app).
- protonplus: 6 == 6 (gtk4 json-glib libadwaita libarchive libgee libsoup3).
- xwayland-satellite-git: xorg-server-xwayland present (+ libxcb/xcb-util-cursor).
- niri-git: wayland + xwayland-satellite-git present (+ wayland libs auto).
- quickshell-git: 8 template deps all present (26 total incl. qt6 stack).
- gcc16 subpkgs: libgcc16-devel pulls glibc libstdc++16; libatomic16-devel pulls
  libatomic16; libstdc++16-devel pulls libstdc++16 - all correct.
- shlib-requires extracted for every package and every lib confirmed resolvable
  (proven by clean fresh installs pulling real deps from Void's repo).

## Security / adversarial audit
- Templates: no curl|sh/wget|sh, no eval/$() injection, no localhost/http
  distfiles, no secrets. `curl|bash` in opencode-schedule.yml is the PINNED
  opencode installer (v1.18.11) - expected. Secrets only via ${{ secrets.* }}.
- GNU_SITE (gcc16) resolves to https ftp.gnu.org - distfiles all HTTPS.
- signing-key.plist public-key == public.pem (RSA 4096), fingerprint matches,
  repo signature verified by xbps during installs. Key pair consistent.

## Issues / PRs
- No open issues. No open pull requests (checked this run).

## Failed-build triage
- Build 31233695050 (niri-git 59a1001) SUCCESS, landed in rolling.
- No failed builds. No residual issues.

## Pending (next run)
- Nothing outstanding. All 19 packages + 8 subpkgs current, consistent,
  installable, and dependency-audited.

## Final state
- Working tree clean. Templates all pass `bash -n`. README consistent.
- .opencode-relay.md updated + pushed (this run_id 31235016513).
