# void-nexus relay state

Status: complete
run_id: 31986752474

## Run context
- 2026-08-17 02:03-03:20 UTC run on main (GITHUB_SHA c990487, RUN_ID
  31986752474). Repo was shallow (1 commit); unshallowed (1256 commits).
- Previous relay (31956620202) had status complete; nothing to pick up.
- check-updates run 31984996964 (SUCCESS, ~01:27) bumped noctalia d0e88fb ->
  bf4da23 and pushed commit c990487 (noctalia binary built by run 31985045368).
- No open issues, no open PRs (checked via gh issue list / gh pr list).

## Package updates - THIS RUN (1 binary published, 1 template bump by us)
### noctalia bf4da23 -> b46cdc1 (us, genuine upstream HEAD move)
- Upstream HEAD moved again to b46cdc1f2236fa2a81b36dc669bbda8a176f2148
  (2026-08-17T02:29:29Z, "build: stop embedding the source assets path").
  check-updates had built bf4da23 at 01:28, but HEAD moved ~1h later.
- Bumped template: _commit=b46cdc1f2236fa2a81b36dc669bbda8a176f2148,
  _shortcommit=b46cdc1, _gitdate=20260817022929,
  version=5.0.0+git20260817022929.b46cdc1, revision=1,
  checksum=2cb682f720721ee733e02ea78f8c561de5db8d04cb51c294d84a0f75f404c639
  (verified by download). bash -n OK.
- Commit a0a872d pushed (author opencode-agent[bot], co-authored 0). Build
  31989029353 SUCCESS. Asset
  noctalia-5.0.0+git20260817022929.b46cdc1_1.x86_64.xbps (+ .sig2) in rolling;
  old bf4da23 purged. Install-tested in clean container (103 dl / 105
  installed / 0 errors; smoke "noctalia v5.0.0"). PASS.
- meson deps diff bf4da23 -> b46cdc1 = NO DIFF (only build-embedding change).

## RACE INCIDENT STATUS (from prior runs, still open)
- Prior runs documented a race: build.yml upload step re-publishes ALL
  packages from BINPKGS (snapshotted at run start) instead of overlaying only
  the target onto current rolling, so a later-finishing build on an older head
  can revert newer binaries.
- THIS RUN: no race incident occurred (single build, relayed sequentially).

## PENDING ACTION - build.yml race fix (blocked AGAIN, 4th run)
- I reconstructed the fix (diff at /tmp/build_yml_fix_thisrun.diff, 150
  lines): rewrite the "Build and Upload Signed Repo to GitHub Release" step to
  (1) baseline public/x86_64 from CURRENT rolling via `gh release download
  rolling -p "*.xbps"`, (2) overlay ONLY TARGET_PKG + its subpackages from
  BINPKGS (dropping stale local copies first), (3) purge stale rolling assets
  for TARGET_PKG + subpackages only (kept the awk substring guard so sibling
  subpackages like noctalia-greeter are NOT deleted when purging noctalia).
  No jq needed (awk used). Validated: YAML OK, all 14 run: blocks pass bash -n.
- PUSH REJECTED: git push token is a GitHub App installation token (ghs_)
  WITHOUT `workflows` permission. Same as prior 3 runs. Commit 94af472 was
  RESET locally; tree clean; diff preserved at /tmp/build_yml_fix_thisrun.diff
  and described above. Reconstruct verbatim from that diff or re-apply the
  recipe in this section.
- Must be pushed by a run/token with workflows write. Until then mitigation:
  verify target asset currency after any concurrent build; re-trigger affected
  build if reverted.

## Version accuracy sweep - THIS RUN (all 19, re-verified)
| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (distfile 200, Blender5.2 dir) | up-to-date |
| brave-browser | 1.93.136 | 1.93.136 (stable releases/latest; v1.95.x are nightly assets) | up-to-date |
| brave-origin-bin | 1.93.136 | 1.93.136 (same release) | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 (releases/latest) | up-to-date |
| gcc16 | 16.1.1+20260801 | snapshot 16-20260815 live | DELIBERATE GAP (manual-only) |
| helium-browser-bin | 0.15.5.1 | 0.15.5.1 (binary reports "Helium 0.15.5.1 (Chromium 151.0.7922.137)") | up-to-date |
| heroic-games-launcher | 2.22.1 | 2.22.1 (app.asar torn == 2.22.1) | up-to-date |
| libspng | 0.7.4 | v0.7.4 (releases/latest) | up-to-date |
| msnap | 0.6.1 | v0.6.1 (releases/latest) | up-to-date |
| niri-git | 26.04+git20260814122556.6062844 | HEAD == 6062844 (ls-remote) | up-to-date |
| noctalia | 5.0.0+git20260817022929.b46cdc1 | HEAD == b46cdc1 (ls-remote) | UPDATED this run |
| noctalia-greeter | 1.2.1 | tag v1.2.1 (latest of 4 tags) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (releases/latest) | up-to-date |
| quickshell-git | 0.3.0+git20260816072204.532a060 | HEAD == 532a060 (ls-remote) | up-to-date |
| rootapp | 0.9.126 | 0.9.126 (AppImage torn: sq.version + X-AppImage-Version 0.9.126; fedora-nexus spec; install URL confirmed live) | up-to-date |
| sdbus-cpp | 2.3.1 (metapackage) | v2.3.1; dep sdbus-c++>=2.3.1_1 resolves (xbps-query verified) | up-to-date |
| vesktop | 1.6.5 | v1.6.5 (app.asar torn == 1.6.5; amd64+arm64 checksums verified) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | HEAD == 3bc915f (ls-remote) | up-to-date |
| zen-browser | 1.21.14b | 1.21.14b (application.ini Version torn; LICENSE verified) | up-to-date |

## Distfile checksum verification - THIS RUN (all 19)
All distfiles re-downloaded and sha256sum'd, all matching template checksums.
- noctalia b46cdc1: 2cb682f7 ✓ (downloaded, matches new template)
- quickshell-git 532a060: 85e8effc ✓ (downloaded, matches template)
- helium-browser-bin 0.15.5.1: f34a1ee1 ✓ (downloaded)
- blender-bin 5.2.0: 96f6c181 ✓ (200)
- brave-browser deb: 9739e5aa ✓ (200)
- brave-origin zip: 0e272651 ✓ (200)
- faugus-launcher 2.1.0: a6956e5b ✓ (200)
- gcc16 all 6 distfiles sha verified by download (gcc snapshot 16-20260801
  2519f6eb; gmp a3c2b8; mpfr b67ba0; mpc ab6424; isl 626335; patches
  108897dc) ✓
- heroic-games-launcher 2.22.1: 8a20af7b ✓ (200)
- libspng 0.7.4: 47ec02be ✓ (200)
- msnap 0.6.1: bfc2ca74 ✓ (200)
- niri-git 6062844: eab3e515 ✓ (200)
- noctalia-greeter 1.2.1: 1d1c4349 ✓ (200)
- protonplus 0.6.4: 46665282 ✓ (200)
- rootapp AppImage: 0736926f ✓ (downloaded + torn)
- sdbus-cpp 2.3.1: distfile n/a (metapackage)
- vesktop deb amd64: 2d4c97ab ✓; arm64: 3ba8e5b ✓ (both downloaded)
- xwayland-satellite-git 3bc915f: 0ae2ac22 ✓ (200)
- zen-browser 1.21.14b: b3a5bb78 ✓ + LICENSE c76f740d ✓ (both 200)

All distfile URLs curl -sI == 200/302.

## Dependency audit - THIS RUN (all 19)
| package | upstream deps found | in template | status |
|---------|--------------------|-------------|--------|
| noctalia b46cdc1 | meson dependency() set diffed vs bf4da23 == identical (cairo, curl, epoxy, fontconfig, freetype2, harfbuzz, jemalloc, libical, libjxl, libqalculate, librsvg, libsecret, libsodium, libwebp, libxml2, md4c, nlohmann, pango, pipewire, polkit, sdbus, sndfile, tomlplusplus, wayland, wireplumber, xkbcommon); BUILDING.md confirms basu-devel sdbus-c++-devel | exact match to makedepends | deps-verified (high) |
| quickshell-git 532a060 | CMake find_package Qt6 Gui/Qml/Quick/QuickControls2/Widgets/ShaderTools/WaylandClient/DBus + QuickPrivate/WaylandClientPrivate; pkg_check_modules JEMALLOC/libdrm/wayland/glib/gobject/pipewire/polkit/polkit_agent | matches makedepends | deps-verified (high) |
| faugus-launcher | python imports GLib/GdkPixbuf/Gio/Gtk, PIL, requests, vdf, psutil, dbus, icoextract; GTK4/libadwaita/libmanette | all exist in Void repos | deps-verified |
| heroic-games-launcher | binary tear: pure Electron bundle, package.json == 2.22.1 | matches | deps-verified |
| rootapp | bundles own libs (libSkiaSharp/libuiohook/libonigwrap/libHarfBuzzSharp); ldd only system base libs | self-contained | deps-verified |
| helium-browser-bin | ldd of chrome/helium: no missing libs; libXScrnSaver verified as correct Void pkg | depends gtk+3 nss alsa-lib xdg-utils libXScrnSaver cups libgcrypt ... | deps-verified |
| msnap | VERSION file == 0.6.1; Makefile uses grim/slurp/gpu-screen-recorder/ffmpeg/jq/notify-send/wl-copy/bash | all in depends | deps-verified |
| protonplus | meson deps appstream, cairo, gee-0.8, gio-unix-2.0, glib-2.0, gtk4, json-glib, libadwaita, libarchive, libnotify, libsoup-3.0, sdl3 | exact match | deps-verified |
| noctalia-greeter | meson deps wlroots-0.20, wayland, xkbcommon, freetype, fontconfig, cairo, pango, librsvg, glib, tomlplusplus, json-c++, libinput, MesaLib, libwebp, stb | exact match to makedepends | deps-verified |
| libspng | meson zlib/miniz deps; zlib-devel in makedepends | matches | deps-verified |
| xwayland-satellite-git | xcb + xcb-util-cursor (libxcb-devel, xcb-util-cursor-devel) | matches | deps-verified |
| niri-git | Cargo.toml pkg-config; features dbus,xdp-gnome-screencast with no-default-features | consistent | deps-verified |
| sdbus-cpp | metapackage; dep sdbus-c++ 2.3.1_1 installs clean | matches | deps-verified |
| all others | unchanged from prior audit; all install-tested clean prior runs | matches | deps-verified (high) |

Built .xbps run_depends torn (props.plist via zstd+tar) for 17 packages - all
match template depends + auto shlib deps.

## Install-self-test - THIS RUN
Method: local repo (rolling *.xbps + sig2 + signing-key.plist), xbps-rindex
-f -a inside container, repo key installed as
b9:f2:38:0f:3f:a7:76:be:5f:ad:01:b9:ef:b5:55:77.plist in /var/db/xbps/keys,
clean ghcr.io/void-linux/void-glibc-full container (NO bash - use sh -c).
- noctalia 5.0.0+git20260817022929.b46cdc1 (new): 103 dl / 105 installed / 0
  errors; smoke "noctalia v5.0.0". PASS.
- quickshell-git 532a060: 174 dl / 175 installed / 0 errors. PASS.
- helium-browser-bin 0.15.5.1: 168 dl / 169 installed / 0 errors; smoke
  "/opt/helium/helium --version". PASS.
- blender-bin 5.2.0: install clean. PASS. brave-browser 1.93.136 (125
  installed): PASS. brave-origin-bin (smoke "Brave Origin 151.1.93.136"):
  PASS. faugus-launcher 2.1.0 (215 installed): PASS. heroic-games-launcher
  2.22.1: PASS. rootapp 0.9.126: PASS. protonplus 0.6.4: PASS. msnap 0.6.1_3
  (170 installed): PASS. libspng 0.7.4_2: PASS. noctalia-greeter 1.2.1_2:
  PASS. sdbus-cpp 2.3.1_2 (pulls sdbus-c++-2.3.1_1): PASS. vesktop 1.6.5 (91
  installed): PASS. zen-browser 1.21.14b: PASS. niri-git (131 installed,
  smoke "niri 26.04"): PASS. xwayland-satellite-git (smoke "-version" ==
  0.8.2; single-dash arg): PASS. gcc16 + 8 subpackages (25 installed) +
  g++16 -std=c++23 flat_set compile+run ("3") + libstdc++16 78 GLIBCXX_3.4.34+
  symbols + noctalia RUNPATH=/opt/gcc16/lib64: PASS.
- Fresh-checker pass: noctalia + helium re-run from fresh container + fresh
  rindex = PASS.

## Rolling release consistency - THIS RUN
- Rolling contains exactly the 27 template binaries (19 main + 8 gcc16
  subpkgs) + sig2 + repodata; no strays. Automated template-vs-rolling check:
  27/27 present, 0 missing. Verified: noctalia b46cdc1 newest, old bf4da23
  purged.
- README package table (19 packages) consistent with srcpkgs.

## Issues / PRs
- No open issues (checked). No open PRs (checked).

## Failed builds
- No failed Build Nexus XBPS or Check and Update runs this run.
  Builds: 31984996964 (check-updates, c990487) SUCCESS; 31985045368 (noctalia
  bf4da23) SUCCESS; 31989029353 (noctalia b46cdc1, ours) SUCCESS.

## Churn audit (NO-CHURN) - THIS RUN
- One template change this run by us (noctalia bf4da23 -> b46cdc1) = genuine
  upstream HEAD move (upstream commits multiple times/day - active project).
  git log confirms every noctalia commit pins a DISTINCT upstream SHA (12
  bumps, 12 unique SHAs; no same-SHA re-bump). All other 18 packages verified
  current and left untouched.
- check-updates.yml guard is a pure version/commit comparison (ls-remote /
  releases API) + fresh re-download for checksum - no churn mechanism.
- lib*16 subpackage templates are regular files with pkgname=gcc16, skipped by
  check-updates (pkgname in gcc16 skip list), identical to gcc16/template.

## gcc16 gap (deliberate, unchanged)
- Upstream snapshot 16-20260815 exists; template pinned 16.1.1+20260801.
  NOT bumped: check-updates.yml excludes gcc16 (multi-hour build, manual
  only). All 6 distfiles re-verified by download this run. Keep this policy.

## not verified / gaps
- GUI/electron apps smoke limited to binary/version/launch in headless
  container (no D-Bus/seat/sandbox/X).
- zen/vesktop/noctalia-greeter aarch64 branches not install-tested.
- gcc16 latest snapshot (16-20260815) not rebuilt (manual-only policy).
- Real-device launch of niri/xwayland-satellite needs Vulkan/display; smoke
  limited to --version / install cleanliness.
- noctalia upstream is very active (multiple commits/day); next run should
  re-check HEAD via ls-remote before relying on b46cdc1 being current.

## Final state
- Working tree clean at end (git status --porcelain empty), HEAD == origin/main.
- noctalia b46cdc1 (a0a872d): build SUCCESS, binary install-tested + published
  to rolling. All 19 packages verified current this run.
- Rolling release coherent with all templates (27/27).
- build.yml race fix still blocked (no workflows-write token this run either,
  4th consecutive run) - see PENDING ACTION. Diff preserved at
  /tmp/build_yml_fix_thisrun.diff.