# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-03 ~21:43 UTC run (opencode-schedule, no relay input).
- Picked up repo at 1f6a35a; unshallowed history (1157 commits) for no-churn audit.

## Completed this run
1. **Failed-build triage**: No failed "Build Nexus XBPS" or "Check and Update Package
   Versions" runs in the last ~20 runs. All recent runs succeeded.
2. **Version inventory sweep (all 19 packages + gcc16 subpkgs) — web/API/artifact verified current.**
   - Git snapshots match upstream default-branch HEAD: niri feb3e43 (26.04),
     quickshell 28771c7 (0.3.0, quickshell-mirror), xwayland-satellite 8d135d3 (0.8.2),
     noctalia 50f0f09 (5.0.0). Git dates verified via GitHub API against _gitdate.
   - Stable latest all confirmed: brave 1.93.129, brave-origin 1.93.129, faugus 2.0.5,
     helium 0.15.1.1, heroic 2.22.0, libspng 0.7.4, msnap 0.6.1, noctalia-greeter 1.1.0
     (v1.1.0 tag), protonplus 0.5.22, sdbus-cpp 2.3.1, vesktop 1.6.5, zen 1.21.10b,
     blender 5.2.0, rootapp 0.9.125.
   - gcc16 = latest announced snapshot 16-20260801 (LATEST-16), distfiles 200 OK.
   - rootapp: AppImage downloaded + torn apart; sq.version=0.9.125,
     X-AppImage-Version=0.9.125, fedora-nexus spec 0.9.125, sha256
     86ed0d76… matches template. VERIFIED across all three references.
3. **Release consistency**: programmatic template-vs-rolling check — 0 missing; all
   expected .x86_64.xbps present incl. gcc16 subpkgs.
4. **Distfiles/checksum verification**: git archives (niri, quickshell, xws, noctalia,
   noctalia-greeter) re-downloaded + sha256 all match; libspng 0.7.4 tarball matches.
5. **Install-test sweep** (rotating, packages not covered in prior run) in
   ghcr.io/void-linux/void-glibc-full: libspng, sdbus-cpp, noctalia-greeter, niri-git,
   quickshell-git, xwayland-satellite-git — ALL install cleanly with no unresolved
   transaction / missing-shlib. niri 26.04, quickshell 0.3.0 run (smoke OK); all heavy
   git packages resolve runtime libs (0 unresolved).
6. **Issues/PRs**: no open issues/PRs. Closed PR #25 (stray auto-generated PR that
   would have deleted .opencode-relay.md; no code changes).
7. **README inventory** consistent with srcpkgs (gcc16 subpkg symlinks excluded).

## Final state
- Working tree clean; no templates/versions changed (nothing was stale or wrong).
- No builds triggered (nothing to build).
- No churn.