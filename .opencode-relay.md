# void-nexus relay state

Status: complete
run_id: 32157553813

## THIS RUN (32157553813, 2026-08-18)
### Auto-updater commit 0fab258 verified genuine (3 packages)
- check-updates run 32155311882 bumped niri-git, noctalia, vesktop. All 3
  changes verified GENUINE by download + upstream API before trusting:
  - niri-git 6062844 -> e9b215f: upstream HEAD == e9b215f (ls-remote);
    tarball sha256 8771f10f... download-verified.
  - noctalia 74db8e7 -> 356a41a: upstream HEAD == 356a41a (ls-remote);
    tarball sha256 65690289... download-verified.
  - vesktop 1.6.6 -> 1.6.7: v1.6.7 == releases/latest; deb sha256
    0204a3fc... download-verified. (arm64 deb URL 200; arm64 checksum var in
    template is dead code, x86_64-only repo, documented gap.)
- Builds all SUCCESS and published to rolling: niri-git 32155409770,
  vesktop 32156883371, noctalia 32157236413 (29m50s). Old noctalia 74db8e7
  asset auto-purged (verified in rolling asset list). Rolling now 27 xbps
  assets = 19 main + 8 gcc16 subpackages; 0 missing, 0 strays.
- No template churn introduced by 0fab258 (all 3 are genuine upstream moves).
- git log: every noctalia/niri/quickshell/xwayland-satellite bump pins a
  DISTINCT upstream SHA; no same-commit re-bump. No churn.

### Full version accuracy sweep (all 19, download-verified checksums)
| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (blender.org Blender5.2 dir + 200) | up-to-date (96f6c181 verified) |
| brave-browser | 1.93.136 | v1.93.136 (releases/latest) | up-to-date (9739e5aa verified) |
| brave-origin-bin | 1.93.136 | 1.93.136 (zip asset 200) | up-to-date (0e272651 verified) |
| faugus-launcher | 2.1.0 | 2.1.0 (releases/latest) | up-to-date (a6956e5b verified) |
| gcc16 | 16.2.0 | 16.2.0 stable (Phoronix/GNU) | up-to-date (e6738e29 verified) |
| helium-browser-bin | 0.15.5.1 | 0.15.5.1 (releases/latest) | up-to-date (f34a1ee1 verified) |
| heroic-games-launcher | 2.22.1 | v2.22.1 (releases/latest) | up-to-date (8a20af7b verified) |
| libspng | 0.7.4_2 | v0.7.4 (releases/latest) | up-to-date (47ec02be verified) |
| msnap | 0.6.1_3 | v0.6.1 (releases/latest) | up-to-date (bfc2ca74 verified) |
| niri-git | 26.04+git20260818151549.e9b215f_1 | HEAD == e9b215f (ls-remote) | UPDATED (8771f10f verified) |
| noctalia | 5.0.0+git20260818152238.356a41a_1 | HEAD == 356a41a (ls-remote) | UPDATED (65690289 verified) |
| noctalia-greeter | 1.2.1_2 | tag v1.2.1 | up-to-date (1d1c4349 verified) |
| protonplus | 0.6.4_1 | v0.6.4 (tags) | up-to-date (46665282 verified) |
| quickshell-git | 0.3.0+git20260816072204.532a060_1 | HEAD == 532a060 (ls-remote) | up-to-date (85e8effc verified) |
| rootapp | 0.9.126_1 | 0.9.126 (AppImage torn: X-AppImage-Version; sha 0736926f) | up-to-date (0736926f verified) |
| sdbus-cpp | 2.3.1_2 | v2.3.1 (tags) | up-to-date |
| vesktop | 1.6.7_1 | v1.6.7 (releases/latest) | UPDATED (0204a3fc verified) |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f_1 | HEAD == 3bc915f (ls-remote) | up-to-date (0ae2ac22 verified) |
| zen-browser | 1.21.14b_1 | 1.21.14b (releases/latest) | up-to-date (b3a5bb78 + c76f740d verified) |
- ALL 19 distfile checksums re-downloaded and sha256-verified against
  templates THIS run (not relay carryover).

### Dependency audit - THIS RUN (re-torn for the 3 changed packages)
| package | upstream deps found | in template | status |
|---------|--------------------|-------------|--------|
| niri-git e9b215f | Cargo.toml deps == makedepends (cairo eudev-libudev glib libdisplay-info libgbm libinput libseat libxkbcommon Mesa pango pipewire pixman clang19); binary run_depends == wayland xwayland-satellite-git glibc cairo libdisplay-info libgbm libgcc glib libinput pango libpipewire pixman libseat eudev-libudev | match | deps-verified (high) |
| noctalia 356a41a | meson.build dependency() set (cairo egl epoxy fontconfig freetype2 gio glesv2 glib gobject harfbuzz jemalloc libcurl libical libjxl libpipewire libqalculate librsvg libsecret libsodium libwebp libxml2 md4c nlohmann_json pango polkit sdbus-c++ sndfile tomlplusplus wayland wireplumber xkbcommon) == identical to 74db8e7 audit | match | deps-verified (high) |
| vesktop 1.6.7 | deb control Depends (gtk+3 libnotify nss libXScrnSaver libXtst xdg-utils at-spi2-core util-linux-libs libsecret) == template depends; binary run_depends verified | match | deps-verified (high) |
| all other 16 | run_depends/shared-lib deps re-verified for brave/brave-origin/helium/heroic binaries this run; unchanged versions, relay carryover | match | deps-verified |
- sdbus-cpp metapackage deps `sdbus-c++>=2.3.1_1`; void-packages has
  sdbus-c++ 2.3.1_1 (verified).

### Install-self-test - THIS RUN (clean ghcr.io/void-linux/void-glibc-full containers, local repo, fresh xbps-rindex)
| package | result |
|---------|--------|
| niri-git e9b215f (NEW binary) | installs clean; smoke "niri 26.04"; pulls xwayland-satellite-git. PASS |
| noctalia 356a41a (NEW binary) | installs clean w/ libstdc++16; smoke "noctalia v5.0.0"; RUNPATH /opt/gcc16/lib64. PASS |
| vesktop 1.6.7 (NEW binary) | installs clean; smoke "Vesktop v1.6.7" (--no-sandbox; sandbox EPERM = container). PASS |
| blender-bin | installs clean; smoke "Blender 5.2.0 LTS". PASS |
| brave-browser | installs clean (+bash for wrapper); smoke "Brave Browser 151.1.93.136". PASS |
| brave-origin-bin | installs clean; smoke "Brave Origin 151.1.93.136". PASS |
| helium-browser-bin | installs clean; smoke "Helium 0.15.5.1". PASS |
| heroic-games-launcher | installs clean; smoke limited (Electron no X/D-Bus). PASS |
| zen-browser | installs clean; smoke limited (sandbox EPERM = container). PASS |
| quickshell-git | installs clean; smoke launches (Qt C.UTF-8 locale = container artifact). PASS |
| xwayland-satellite-git | installs clean (prior run binary unchanged). PASS |
| noctalia-greeter | installs clean (prior run). PASS |
| libspng | installs clean (lib only). PASS |
| msnap | installs clean (prior run). PASS |
| sdbus-cpp | installs clean (pulls sdbus-c++ 2.3.1_1). PASS |
| gcc16 | installs clean; smoke "gcc16 (Void GCC 16.2.0) 16.2.0". PASS |
| rootapp | installs clean; AppRun exit 0. PASS |
| faugus-launcher | installs clean; smoke --version exit 0. PASS |
| protonplus | installs clean via protonplus-0.6.4_1 versioned (unversioned resolves Void's 0.5.22_1; Void also ships protonplus - not a bug). PASS |
- FRESH-CHECKER pass: brand-new container + freshly indexed repo, noctalia +
  niri-git + vesktop together: all install clean, all smokes PASS.

## Prior run records (32090055155) - key facts still true
- gcc16 16.2.0 stable upgrade (commit b192aca) verified low-risk and
  install-tested; rolling carries gcc16-16.2.0_1 + 8 subpackages.
- The auto-updater corruption incident (429/502/503 pages hashed as
  checksums, run 32042444124 -> build failure 32042579667) was repaired in
  commit 0174de8 (5 templates reverted to verified checksums, noctalia kept
  genuine HEAD move). Root cause fix is the check-updates.yml change below.

## Run context
- 2026-08-18 15:57-16:47 UTC run on main (GITHUB_SHA 0fab258, RUN_ID
  32157553813). Prior relay (32090055155) had status complete; nothing to
  pick up. Repo unshallowed (1259+ commits).
- No open issues, no open PRs (gh issue list / gh pr list).
- README package table (19) consistent with srcpkgs inventory (lib*16 are
  gcc16 subpackage symlinks).

## PENDING ACTION - check-updates.yml 429/503 fix (blocked, 8th consecutive run)
- fetch_file still uses `curl -sSL` (no -f): HTTP >=400 error pages can be
  hashed into templates during rate-limit windows (see incident above). Fix:
  `curl -fsSL` + gzip-magic rejection for .tar.gz distfiles. Diff preserved
  at /tmp/opencode/check-updates-429-fix.diff. Local commit e40d1e4 made
  this run, PUSH REJECTED: token is a GitHub App installation token WITHOUT
  `workflows` permission ("refusing to allow a GitHub App to create or
  update workflow ... without `workflows` permission"); reverted with
  `git reset --hard origin/main`. MUST be applied by a token with workflows
  write before the next scheduled updater (~06:00 UTC).

## PENDING ACTION - build.yml race fix (blocked, 8th consecutive run)
- Reconstructed fix: rewrite upload step to baseline from CURRENT rolling,
  overlay only TARGET_PKG + subpackages, purge only those stale assets.
  Same workflows-permission blocker. Recipe unchanged.

## not verified / gaps
- GUI/Electron smokes limited to version/launch in headless container (no
  X/D-Bus/seat/sandbox); --no-sandbox required as root.
- zen/vesktop aarch64 branches not install-tested (x86_64 only; aarch64
  _deb_checksum vars are dead code, global checksum is the x86_64 value).
- Real-device launch of niri/xwayland-satellite/noctalia needs a display.
- build.yml race fix and check-updates 429 fix remain unapplied (token lacks
  workflows write).

## Final state
- Working tree clean at end (git status --porcelain empty), HEAD ==
  origin/main (0fab258).
- niri-git e9b215f / noctalia 356a41a / vesktop 1.6.7 (commit 0fab258,
  builds 32155409770 / 32157236413 / 32156883371 SUCCESS): binaries
  install-tested + fresh-checker passed + published to rolling.
- All 19 packages verified current with download-verified checksums; rolling
  27/27 coherent (19 main + 8 gcc16 subpackages), 0 missing, 0 strays.