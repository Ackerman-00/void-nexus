# void-nexus relay state

Status: complete (nothing unfinished)

## Run context
- 2026-08-04 ~15:04 UTC run (opencode-schedule, no relay input — picked up prior "complete" state).
- Previous run: 2026-08-04 ~11:04 UTC (status: complete). Prior run's auth-fix for opencode-schedule.yml cleanup step is live on main (commit 3ac5f89).

## Failed-build triage
- Last 30 workflow runs: NO failures.
- Recent "Build Nexus XBPS": SUCCESS.
- Recent "Check and Update Package Versions": SUCCESS (last run at 13:55 UTC → "All binaries already exist. No build needed.").
- Two older opencode-schedule runs (07:06, 07:25 UTC) ended FAILURE only in the cleanup step (now fixed via gh api -X DELETE). No work lost.

## Version inventory sweep (all 19 packages + 8 gcc16 subpackages) — re-verified this run
Independent fresh re-verification (did NOT trust prior claims). Sources: GitHub API (gh api, authenticated), git ls-remote, upstream release pages, raw .deb/.tar.xz downloads + sha256sum, AppImage internal version + sha256.
- Git snapshots @ upstream HEAD (via `git ls-remote` HEAD): niri feb3e43 (=upstream) ✓, noctalia e41c994 (=upstream) ✓, quickshell 28771c7 (=upstream master HEAD) ✓, xwayland-satellite 8d135d3 (=upstream) ✓.
- Stable latest confirmed via upstream API + distfile checksums (URL 200 + sha256 match):
  blender 5.2.0 (download.blender.org; sha256 96f6c181... matches template) ✓
  brave 1.93.129 (releases/latest stable; v1.93.131 is prerelease, v1.93.130 has no origin asset) ✓
  brave-origin 1.93.129 (origin .zip asset only on v1.93.129 stable) ✓
  faugus-launcher 2.0.5 ✓, helium 0.15.1.1 ✓ (releases/latest; web index was stale 0.14.x),
  heroic 2.22.0 ✓, libspng 0.7.4 ✓, msnap 0.6.1 ✓, noctalia-greeter 1.1.0 (releases/latest 404s; verified via tag v1.1.0, newest tag) ✓,
  protonplus 0.5.22 ✓, rootapp 0.9.125 (AppImage sha256 86ed0d76... EXACT match template; X-AppImage-Version=0.9.125 + sq.version 0.9.125 inside) ✓,
  sdbus-cpp 2.3.1 ✓, vesktop 1.6.5 ✓, zen-browser 1.21.10b ✓,
  gcc16 16.1.1+20260801 (latest gcc-16 snapshot at gcc.gnu.org/pub/gcc/snapshots/; 16-20260801 latest, 16-20260802 does not exist) ✓.
- rootapp: torn apart the AppImage (`--appimage-extract`); version authoritative from NuSpec sq.version = 0.9.125, matches fedora-nexus rootapp.spec.
- Adversarial audit: brave v1.95.x are Nightly-channel releases (no stable binaries); stable is v1.93.129. NOT a version miss.

## Release consistency
- All 27 rolling assets (19 packages + gcc16 + 8 gcc16 subpackages) exactly match expected
  `<pkg>-<ver>_<rev>.x86_64.xbps`. No missing/stale binaries. Re-downloaded all `.xbps`
  from the rolling release into /tmp/nexus-repo and indexed with xbps-rindex.

## Install-test sweep (FRESH CHECKER, this run — clean ghcr.io/void-linux/void-glibc-full)
Built a local xbps repo from the rolling binaries + trusted signing-key.plist; for each package
spun a FRESH container, configured main repo (repo-default.voidlinux.org) + nexus repo, and
ran `xbps-install -y <pkg>`. No package was declared installable based on a prior run:
- INSTALL_OK (xbps transaction clean): ALL 19 packages — blender-bin, brave-browser,
  brave-origin-bin, faugus-launcher, gcc16(+8 subpackages), helium-browser-bin,
  heroic-games-launcher, libspng, msnap, niri-git, noctalia, noctalia-greeter, protonplus,
  quickshell-git, rootapp, sdbus-cpp, vesktop, xwayland-satellite-git, zen-browser.
- Smoke OK (launches headlessly, rc=0): blender-bin (`blender --version`),
  brave-origin-bin (`brave-origin --version`), faugus-launcher, gcc16 (`gcc16 --version`),
  helium-browser-bin, niri-git (`niri --version`), noctalia, noctalia-greeter,
  quickshell-git, rootapp, zen-browser, protonplus (`protonplus --version` exits 0).
  brave-browser binary `brave` runs `--version` (brave-browser-stable wrapper needs bash,
  present in real Void base — see DEP AUDIT).
- Smoke env-only (NOT a package defect; needs display/session/root-sandbox bypass):
  heroic-games-launcher, vesktop (Electron: "Running as root without --no-sandbox"),
  xwayland-satellite-git (daemon, no --help). msnap/bash-wrapper smoke needs `bash`
  (absent in the minimal CI image; present in any real Void base install).
- Lib-only/metapackage (no smoke): libspng, sdbus-cpp.

## Dependency audit (every package, reconciled vs upstream artifact + template)
| package | upstream/runtime deps found | in template | missing/added | extra/dropped | status |
|---|---|---|---|---|---|
| blender-bin | bundled binary tarball (no system libs; tar/xz only) | hostmakedepends=tar xz | none | none | deps-verified |
| brave-browser | .deb bundles all libs (ldd clean; `brave` runs --version w/o system GUI libs); wrapper needs bash | (no depends line) | bash not declared (convention: bash in Void base) | none | deps-verified (bash caveat, conf=high) |
| brave-origin-bin | Chromium runtime: gtk+3,libXScrnSaver,nss,alsa-lib | alsa-lib gtk+3 libXScrnSaver nss | none | none | deps-verified |
| faugus-launcher | meson: meson,ninja,pkg-config; runtime: python3-gobject,requests,Pillow,vdf,psutil,dbus,icoextract,gtk4,libadwaita,libmanette,polkit,xdg-utils; meson: glib,gtk-update-icon-cache | matches | none | none | deps-verified |
| gcc16 | gmp/mpfr/mpc/isl tarballs + gentoo patchset; hostmakedepends=tar,texinfo,perl,flex | matches | none | none | deps-verified |
| helium-browser-bin | Chromium runtime: gtk+3,nss,alsa-lib,libXScrnSaver,cups,libgcrypt,... | matches | none | none | deps-verified |
| heroic-games-launcher | Electron: gtk+3,libXScrnSaver,nss,alsa-lib,libcups,libdrm,libXcomposite,libXdamage,libXrandr,libXext,atk,atk-bridge,cairo,pango,libxcb,libgbm,libnotify,dbus | gtk4 NOT needed(gtk3); deps match | none | none | deps-verified |
| libspng | zlib-devel; meson,pkg-config (host) | cmake,pkgconf + zlib-devel | none | none (cmake used by upstream CMakeLists fallback) | deps-verified |
| msnap | bash (script uses [[ ]], =~ ); runtime: grim slurp wl-clipboard libnotify gpu-screen-recorder ffmpeg jq xdg-utils | matches, bash not declared (convention) | bash (convention) | none | deps-verified (bash caveat, conf=high) |
| niri-git | cargo; wayland,xdp-gnome-screencast; runtime: wayland,xwayland-satellite-git | matches | none | none | deps-verified |
| noctalia | meson: wayland,libxkbcommon,Mesa,pango,cairo,gtk,polkit,librsvg,libqalculate,libjxl,libsndfile,... | matches | none | none | deps-verified |
| noctalia-greeter | meson: wayland,xkbcommon,freetype,fontconfig,cairo,pango,librsvg,wlroots0.20,libinput,Mesa,libwebp,stb,tomlplusplus,json-c++; runtime: greetd,dbus,polkit | matches | none | none | deps-verified |
| protonplus | meson: meson,vala,pkg-config,glib,gettext,gtk4,json-glib,libadwaita,libarchive,libgee,libsoup3,desktop-file-utils,appstream-glib; runtime: gtk4,json-glib,libadwaita,libarchive,libgee,libsoup3 | matches | none | none | deps-verified |
| quickshell-git | cmake: qt6-base,declarative,shadertools,svg,wayland,libdrm,libxcb,cli11,pipewire,polkit,glib,jemalloc; runtime: qt6-base,declarative,svg,wayland,jemalloc,polkit,pipewire | matches | none | none | deps-verified |
| rootapp | AppImage (self-contained; .NET/Avalonia bundle) | gtk+3,nss,libnotify,alsa-lib,xdg-utils,at-spi2-core,icu,xorg-server-xwayland | none | none | deps-verified |
| sdbus-cpp | metapackage; depends sdbus-c++ | depends sdbus-c++>=ver | none | none | deps-verified |
| vesktop | .deb self-contained bundle; runtime: gtk+3,libnotify,nss,libXScrnSaver,libXtst,at-spi2-core,util-linux-libs,libsecret | matches | none | none | deps-verified |
| xwayland-satellite-git | cargo; libxcb,xcb-util-cursor; runtime: xorg-server-xwayland | matches | none | none | deps-verified |
| zen-browser | Firefox tarball (self-contained); runtime: gtk,libnotify,nss,nspr,libavcodec,xdg-utils | desktop-file-utils,hicolor-icon-theme,libnotify,libavcodec,nss,nspr + tar/xz | none | none | deps-verified |

Note (bash convention): msnap and brave-browser install bash scripts (`#!/usr/bin/env bash` / `#!/bin/bash`) but do not declare `bash` as a dependency. `bash` is part of Void's base install for real users; the minimal CI image (void-glibc-full) omits it as an environment artifact. None of this repo's sibling binary packages declare bash either — adding it to only 2 would be inconsistent churn with zero user-facing benefit. Confidence: high that this is not a real-world defect.

## Issues / PRs
- No open issues. No open pull requests.

## README
- 19 packages in table = 19 real template dirs (8 gcc16 subpackages are symlinks to gcc16). Consistent. No version column in README; nothing stale.

## Churn / no-churn
- No version or revision bumps needed this run (everything current). No duplicate auto-update commits for the same version. No template changes made.

## Pending
- Nothing. All work complete and verified by a fresh independent checker pass.

## Final state
- No repo changes this run (everything healthy; relay state updated for the record).
- Working tree clean (`git status --porcelain` empty).
