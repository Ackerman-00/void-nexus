# void-nexus relay state

Status: complete
run_id: 32043474138

## Run context
- 2026-08-17 15:49-17:00 UTC run on main (GITHUB_SHA 0174de8, RUN_ID
  32043474138). Repo was shallow (1 commit); unshallowed (1259 commits).
- Previous relay (31986752474) had status complete; nothing to pick up.
- No open issues, no open PRs (checked via gh issue list / gh pr list twice).

## CRITICAL INCIDENT THIS RUN: auto-updater corrupted 6 templates
- Build run 32042579667 (Build Nexus XBPS, 15:31) FAILED on
  xwayland-satellite-git with "SHA256 mismatch". Root cause: check-updates
  run 32042444124 (15:28, SUCCESS) committed 03b9d95 which wrote the sha256
  of a GitHub rate-limit/error page (6850b0e5... = 429 page; de78a2a1... =
  502 page) as the checksum + revision bump for 6 packages:
  faugus-launcher, libspng, msnap, niri-git, noctalia, xwayland-satellite-git.
- PROOF: I downloaded the SAME github.com/.../archive URLs and got both the
  real archive (0ae2ac22, e13b83a5, etc.) and error pages (429 -> 6850b0e5,
  502 -> de78a2a1, 503) on alternating requests. The bogus checksums in the
  corrupted templates hash EXACTLY to those error pages.
- check-updates.yml's fetch_file used `curl -sSL` (no -f), so HTTP >=400 was
  treated as success and the error page was hashed into the template.

## FIXES THIS RUN
### Template repairs (commit 0174de8, pushed to main)
Reverted bogus revision+checksum churn on 5 packages whose upstream did NOT
change (real archive checksums re-verified by download this run):
- faugus-launcher 2.1.0: rev 2->1, checksum a6956e5b (download-verified)
- libspng 0.7.4: rev 3->2, checksum 47ec02be (download-verified)
- msnap 0.6.1: rev 4->3, checksum bfc2ca74 (download-verified)
- niri-git 6062844: rev 2->1, checksum eab3e515 (download-verified)
- xwayland-satellite-git 3bc915f: rev 2->1, checksum 0ae2ac22
  (download-verified 3x)
noctalia: KEPT the genuine upstream HEAD move b46cdc1 -> b10dd55
(ls-remote + API verified: upstream HEAD == b10dd55, committed
2026-08-17T11:47:29Z); corrected checksum to e13b83a5 (real archive,
3x download-verified).
- bash -n passed on all 6 templates. Identity verified before/after commit
  (opencode-agent[bot], co-authored 0).

### Updater root-cause fix (BLOCKED - 5th consecutive run)
- Fixed check-updates.yml fetch_file to use `curl -fsSL` (fail on HTTP
  >=400) + reject non-gzip content for .tar.gz distfiles. Diff preserved at
  /tmp/opencode/check-updates-429-fix.diff (25 lines). YAML + embedded
  python syntax validated.
- PUSH REJECTED: token is a GitHub App installation token WITHOUT
  `workflows` permission (git push AND gh api PUT contents both rejected:
  "refusing to allow a GitHub App to create or update workflow ... without
  `workflows` permission" / "Resource not accessible by integration" 403).
- RISK: the NEXT check-updates run (~06:00 UTC) still has the buggy
  fetch_file. If the runner IP gets rate-limited (429/502/503 on archive
  downloads), it will corrupt checksums again. Mitigation in place: all
  template checksums are now the verified-correct values; a subsequent run
  MUST re-apply the /tmp diff or the recipe above. The fix must be pushed
  by a token with workflows write.

### Build re-trigger
- noctalia (new commit b10dd55): first trigger 32044088195 FAILED on
  transient GitHub 503 (xbps-fetch "Service Unavailable"; template checksum
  correct). Retry 32044302265 SUCCESS. Binary
  noctalia-5.0.0+git20260817114729.b10dd55_1.x86_64.xbps published to
  rolling; old b46cdc1 asset purged (verified in rolling asset list).

## Version accuracy sweep - THIS RUN (all 19, re-verified)
| package | packaged version | upstream latest | status |
|---------|------------------|-----------------|--------|
| blender-bin | 5.2.0 | 5.2.0 (download.blender.org Blender5.2 dir + file 200) | up-to-date |
| brave-browser | 1.93.136 | v1.93.136 (releases/latest) | up-to-date |
| brave-origin-bin | 1.93.136 | 1.93.136 (brave-origin-*-linux-amd64.zip asset 200) | up-to-date |
| faugus-launcher | 2.1.0 | 2.1.0 (releases/latest + tag) | up-to-date (checksum fixed) |
| gcc16 | 16.1.1+20260801 | snapshot 16-20260815 live | DELIBERATE GAP (manual-only) |
| helium-browser-bin | 0.15.5.1 | 0.15.5.1 (imputnet/helium-linux releases/latest) | up-to-date |
| heroic-games-launcher | 2.22.1 | v2.22.1 (releases/latest) | up-to-date |
| libspng | 0.7.4 | v0.7.4 (releases/latest) | up-to-date (checksum fixed) |
| msnap | 0.6.1 | v0.6.1 (releases/latest) | up-to-date (checksum fixed) |
| niri-git | 26.04+git20260814122556.6062844 | HEAD == 6062844 (ls-remote) | up-to-date (checksum fixed) |
| noctalia | 5.0.0+git20260817114729.b10dd55 | HEAD == b10dd55 (ls-remote) | UPDATED (checksum fixed) |
| noctalia-greeter | 1.2.1 | tag v1.2.1 (latest of tags) | up-to-date |
| protonplus | 0.6.4 | v0.6.4 (tags) | up-to-date |
| quickshell-git | 0.3.0+git20260816072204.532a060 | HEAD == 532a060 (ls-remote) | up-to-date |
| rootapp | 0.9.126 | 0.9.126 (AppImage torn: X-AppImage-Version + sq.version == 0.9.126; sha256 0736926f == template; fedora-nexus spec == 0.9.126) | up-to-date |
| sdbus-cpp | 2.3.1 | v2.3.1 (tags) | up-to-date |
| vesktop | 1.6.5 | v1.6.5 (releases/latest) | up-to-date |
| xwayland-satellite-git | 0.8.2+git20260808203058.3bc915f | HEAD == 3bc915f (ls-remote) | up-to-date (checksum fixed) |
| zen-browser | 1.21.14b | 1.21.14b (releases/latest) | up-to-date |

## Distfile URL/checksum verification - THIS RUN
- All distfiles URLs curl -I -L == 200 (vesktop deb v1.6.5_amd64, zen tar +
  LICENSE, brave deb/zip, helium, heroic, blender, faugus, libspng, msnap,
  noctalia-greeter, protonplus).
- Checksums re-verified by download this run for the 6 touched packages
  (see FIXES). rootapp AppImage downloaded + torn (sha 0736926f matches).
- For the 13 non-touched packages: version unchanged since last run's
  full-download checksum verification; not re-downloaded this run (gap).

## Dependency audit - THIS RUN
| package | upstream deps found | in template | status |
|---------|--------------------|-------------|--------|
| noctalia b10dd55 | RE-TEAR: meson.build dependency() set (cairo, cairo-ft, egl, epoxy, fontconfig, freetype2, gio, glesv2, glib, gobject, harfbuzz, jemalloc, libcurl, libical, libjxl, libjxl_threads, libpipewire, libqalculate, librsvg, libsecret, libsodium, libwebp, libxml2, md4c, nlohmann_json, pango, pangocairo, pangoft2, polkit-*, sdbus-c++, sndfile, tomlplusplus, wayland-*, wireplumber, xkbcommon) == IDENTICAL to b46cdc1 audit | exact match to makedepends | deps-verified (high) |
| faugus-launcher | binary props.plist run_depends == template depends (python3-* gtk4 libadwaita libmanette polkit xdg-utils) | match | deps-verified |
| libspng | run_depends glibc zlib + shlib | match | deps-verified |
| msnap | run_depends bash grim slurp wl-clipboard libnotify gpu-screen-recorder ffmpeg jq xdg-utils | match | deps-verified |
| niri-git | run_depends wayland xwayland-satellite-git + shlib | match | deps-verified |
| xwayland-satellite-git | run_depends xorg-server-xwayland + shlib | match | deps-verified |
| all other 13 | unchanged from prior run audits (same versions) | match | deps-verified (relay carryover) |

## Install-self-test - THIS RUN (clean ghcr.io/void-linux/void-glibc-full, local repo + xbps-install -S for main deps, bash added for wrapper smoke)
| package | result |
|---------|--------|
| noctalia b10dd55 (NEW binary) | 103 dl / 105 installed / 0 errors; smoke "noctalia v5.0.0"; ldd clean. PASS |
| niri-git 6062844 | 129 dl / 131 installed / 0 errors; smoke "niri 26.04"; pulls xwayland-satellite-git from repo. PASS |
| quickshell-git | install clean; smoke launches (Qt C.UTF-8 locale warning = container artifact). PASS |
| xwayland-satellite-git | install clean; smoke "-version" == 0.8.2 (single-dash arg). PASS |
| brave-browser | 124 dl / 125 installed; smoke "Brave Browser 151.1.93.136". PASS |
| brave-origin-bin | smoke "Brave Origin 151.1.93.136". PASS |
| helium-browser-bin | smoke "Helium 0.15.5.1 (Chromium 151.0.7922.137)". PASS |
| vesktop | smoke "Vesktop v1.6.5"; ldd libffmpeg "not found" is EXPECTED (bundled /opt/Vesktop/libffmpeg.so). PASS |
| zen-browser | smoke "Mozilla Zen 1.21.14b" (sandbox EPERM = container). PASS |
| rootapp | launches (RootApp info log); ldd clean. PASS |
| heroic-games-launcher | install clean; smoke limited (Electron: no X/D-Bus in headless). PASS |
| faugus-launcher | install clean; smoke --version exit 0. PASS |
| protonplus | install clean; smoke shows usage (--version unsupported, exit 0). PASS |
| msnap | 169 dl / 170 installed; smoke usage exit 0; ldd clean. PASS |
| libspng | 0 dl / 1 installed (lib only). PASS |
| noctalia-greeter | 65 dl / 66 installed. PASS |
| sdbus-cpp | 2 dl / 3 installed (pulls sdbus-c++ 2.3.1_1). PASS |
| blender-bin | installed; smoke via /opt/blender/blender. PASS |
| gcc16 | install clean; smoke gcc16 --version; ldd clean. PASS |
- FRESH-CHECKER pass: from a completely fresh download of rolling into a
  new dir + fresh container: noctalia b10dd55 (103 dl/105 inst, smoke v5.0.0)
  PASS; niri-git (129 dl/131 inst, pulls xwayland-satellite-git) PASS.

## Rolling release consistency - THIS RUN
- Automated template-vs-rolling check: 27/27 present, 0 missing, 0 strays.
  noctalia b10dd55 newest, old b46cdc1 purged (verified in asset list).
- README package table (19) consistent with srcpkgs.

## Churn audit - THIS RUN
- The 5 reverts removed churn the auto-updater had introduced (revision
  bumps with no upstream change). noctalia bump is a genuine HEAD move.
- git log: every noctalia/niri/quickshell/xwayland-satellite bump pins a
  DISTINCT upstream SHA; no same-commit re-bump. No churn mechanism in
  check-updates.yml beyond the (now-blocked) 429 fix.

## PENDING ACTION - build.yml race fix (blocked, 5th run)
- Reconstructed fix (prior runs): rewrite the upload step to baseline from
  CURRENT rolling, overlay only TARGET_PKG + subpackages, purge only those
  stale assets. Previously validated YAML + bash -n; push rejected for
  missing `workflows` permission. Same blocker as this run. Diff not
  regenerated this run; recipe unchanged.

## PENDING ACTION - check-updates.yml 429/503 fix (blocked, THIS run)
- MUST be applied by a token with workflows write before the next scheduled
  updater (~06:00 UTC) to prevent repeat template corruption. Diff:
  /tmp/opencode/check-updates-429-fix.diff (25 lines): fetch_file -> curl
  -fsSL + gzip-magic rejection for .tar.gz. Re-verify YAML/python syntax
  before pushing.

## not verified / gaps
- 13 non-touched packages' distfile checksums not re-downloaded this run
  (versions unchanged since last run's full verification).
- GUI/Electron smoke limited to version/launch in headless container (no
  X/D-Bus/seat/sandbox).
- zen/vesktop aarch64 branches not install-tested (x86_64 only).
- gcc16 latest snapshot (16-20260815) not rebuilt (manual-only policy).
- Real-device launch of niri/xwayland-satellite/noctalia needs a display;
  smoke limited to --version/launch + install cleanliness.
- build.yml race fix and check-updates 429 fix remain unapplied (token lacks
  workflows write).

## Final state
- Working tree clean at end (git status --porcelain empty), HEAD ==
  origin/main (0174de8).
- noctalia b10dd55 (commit 0174de8, build 32044302265 SUCCESS): binary
  install-tested + fresh-checker passed + published to rolling.
- All 19 packages verified current; rolling 27/27 coherent.
