# Template file for 'noctalia-v5'
pkgname=noctalia-v5
version=20260514
revision=2

_gitbranch="v5"
_commit=4d8f04c6f08ea9c6e7b3b9b0f6dd41329b8be2e0
wrksrc="noctalia-shell-${_commit}"

build_style=meson
buildtype=release
configure_args="-Db_ndebug=true"
short_desc="Lightweight Wayland shell built directly on Wayland and OpenGL ES"
maintainer="Noctalia <team@noctalia.dev>"
license="MIT"
homepage="https://github.com/noctalia-dev/noctalia-shell"

distfiles="https://github.com/noctalia-dev/noctalia-shell/archive/${_commit}.tar.gz"
checksum="908213ed34a8121275d0ce2507e4c2a8b92ce7c5c439c1d1d3c8cc922b6d1895"

# Conflict with 'noctalia' to prevent package collisions
conflicts="noctalia>=0"
provides="noctalia-${version}_${revision}"

CXXFLAGS="-std=c++20"

hostmakedepends="meson ninja pkg-config git curl"

makedepends="
 wayland-devel
 wayland-protocols
 libglvnd-devel
 libepoxy-devel
 cairo-devel
 pango-devel
 fontconfig-devel
 freetype-devel
 harfbuzz-devel
 libxkbcommon-devel
 pipewire-devel
 libcurl-devel
 pam-devel
 libwebp-devel
 basu-devel
 sdbus-c++-devel
 polkit-devel
 librsvg-devel
 jemalloc-devel
 glib-devel
"

post_install() {
    vlicense LICENSE
    vdoc README.md

    if [ ! -x "${DESTDIR}/usr/bin/noctalia" ]; then
        msg_error "Expected binary /usr/bin/noctalia not found.\n"
    fi
}
